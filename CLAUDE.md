# tradedesk

Short-term momentum scanner for NSE stocks using the INDstocks (INDmoney) API.
Evening scan on daily charts, live trigger monitoring, trades held hours to 10 sessions.
It alerts; the human places every order.

**Status: M1-M11 built and unit-tested. Live credentials in place (`tradedesk auth setup`, keychain). M2 PASS: full-universe 10y history load, 0 errors/429s; daily close matches NSE's official close to the paisa; found and fixed a real API defect (silent data loss on narrow candle windows near "now" - commit 67f39c9). Close-finality timing (PLAN.md 5.2) scheduled unattended for the next trading day, not yet measured. M3 PASS: quality report reviewed on 2,639 real codes (issues attributable to late listings/illiquid names/one feed anomaly, not pipeline bugs); split/bonus days don't crash; universe-by-date computes cleanly. Corporate-actions (22,694) and results-calendar (97,163) CSVs imported from NSE's own API - see the corporate-actions adjustment note below, the actions table is NOT applied by default. Full writeup: docs/signoff-m2-m3.md. M1 cost model verified against 5 FY25-26 ledger bills (delivery, paisa-exact); intraday has NO real note yet - provisionally covered by tests/golden/test_rate_card.py (rate-card examples, not a real bill; replace with a real note the moment one exists). M4 indicator check passes provisionally against an independent hand-derived reference (no TradingView premium available - see tests/golden/indicators/README.md); replace with a real TradingView export if premium access exists later.

M13 (crypto, CoinDCX) Phases 1-4 done: markets/ abstraction (CostModel; NSE ported with zero behaviour change), broker/coindcx/ (read-only, no auth needed), `--market crypto` on data sync-instruments/load/quality/universe/status AND scan/backtest (own store: data/crypto.duckdb). 338 INR pairs loaded, 340,108 bars, 0 errors. CryptoCostModel: 0.2% maker/taker base tier (corroborated via search, CoinDCX's fee page is a client-rendered SPA), 1% TDS (Section 194S) on the sell leg only, 18% GST on the fee not the TDS, no STT/stamp/DP/SEBI equivalent. config/markets/crypto.yaml's turnover floor (Rs 25 lakh) is data-informed from the real 338-pair distribution, not a guess. Verified end-to-end on real data (docs/signoff-crypto-phase4.md): a real scanned signal's net R:R went NEGATIVE at target on a stablecoin (TDS+fees exceeding the whole gross reward) - the cost figure hand-checked to the paisa. Found and fixed a real crash running it for real: some of CoinDCX's early (2019-2020) history has a duplicate-calendar-day phantom bar (synthetic, flat, zero-volume, ~1s before the real one) that broke prepare_market's reindex - `_dedupe_by_calendar_day()` in backtest/runner.py fixes it, no-op for NSE. FIXED 2026-09-12 (fractional sizing): risk/sizing.py now quantizes to a `qty_step` (NSE 1.0 = whole shares, proven identical to the old math.floor path over 20k random cases + a hypothesis property test; crypto fractional) and enforces CoinDCX's flat Rs 100 min_notional. Before this, BTC/ETH/BNB had NEVER been backtested even once - every signal was silently skipped as "size 0", so the earlier "majors" result was really a verdict on cheap altcoins only. **With them included the answer is clear and negative: 253 trades over 2018-2026 on 22 large caps, gross P&L -Rs 5,198 (i.e. no edge before costs) and costs -Rs 20,272 on top, for -0.515R expectancy and a 26% drawdown. A round trip costs 1.47% of position value, of which the 1% Section 194S TDS on the sell leg is 68% - charged on sell VALUE, win or lose, so a ~3-day hold must gain 1.47% just to break even. BTC (-0.75R, 85 trades) and ETH (-0.55R, 58) were the WORST performers, not the best. No setup is positive gross except trend_pullback (+Rs 27/trade over only 30 trades, still net-negative after its own costs). Conclusion: do NOT trade these setups on CoinDCX INR pairs; this is structural (a turnover tax), not a tuning problem. See data/reports/crypto_majors_fractional.csv.** Paper trading and the live risk manager stay NSE-only on purpose.** Next: NSE paper trading (crypto has no edge to trade); M12 (order placement) only after Phase 3 of the ORIGINAL plan (unrelated numbering to M13's phases - see PLAN.md). Full spec and milestone list: PLAN.md.

**Unattended automation (2026-09-12), 13 Windows Scheduled Tasks - `Get-ScheduledTask -TaskName tradedesk-*`:** close-finality, dashboard, live-session, after-close (5 steps as of 2026-09-13, the last being `ml check-drift`), crypto-tracker (every 30 min as of 2026-09-13), bse-tracker, bse-after-close, bse-live-session, options-snapshot, research-tracker (NSE, added 2026-09-13), crypto-research-tracker and bse-research-tracker (added 2026-09-14), crypto-eod-learning (added 2026-09-14, crypto's second daily EOD-learning pass) - see entries below for each.
- `tradedesk-close-finality` - one-shot, already covered above.
- `tradedesk-live-session` - Mon-Fri 09:10 IST: `tradedesk live --until 15:35 --no-dashboard` on the newest saved watchlist. This is what actually populates the journal with real triggers - `paper update` has nothing to simulate without it. Runs WITHOUT its embedded dashboard on purpose - `tradedesk-dashboard` below owns port 8765 so the two never race for the socket.
- `tradedesk-dashboard` (added 2026-09-12) - `AtLogOn` trigger, no end time, `RestartCount 999`/`RestartInterval 1m` - runs `tradedesk dashboard` forever so http://127.0.0.1:8765 always resolves to something, not just during the 09:10-15:35 window. The dashboard command now self-refreshes (`--refresh-seconds`, default 60; disabled if you pin both `--watchlist` and `--session` since there's then nothing to discover): it re-scans `data/watchlists/` and today's `data/sessions/<date>.jsonl` on a timer and reloads on an mtime change, so a call that triggers mid-day shows up without restarting the process - needed precisely because `tradedesk-live-session` no longer pushes to it directly. UI was also decluttered same day per user request ("only show calls given by agent and its report"): the Today tab dropped Prices/Alerts/health-light panels and merged Watchlist+Signals into one "Today's calls" table (plan + live status side by side, e.g. `planned`/`triggered`/`stopped`); tabs renamed Calls/Report. `index.html`'s DOM ids changed (`#calls` replaces `#watchlist`+`#signals`) but the JSON APIs (`/api/state`, `/api/eod`, `/api/performance`) are untouched, so existing tests are unaffected.
- **BSE (added 2026-09-12)** - a third market, and a much lighter lift than crypto: BSE equities trade through the SAME INDstocks broker/credentials as NSE, just a different `EXCH` value in the same instrument-master CSV (`source=equity` already returns both exchanges in one file) and a `BSE_<security_id>` scrip-code prefix instead of `NSE_`. Verified live before writing any code: `tradedesk quote BSE_500325` and `tradedesk candles BSE_40000006 --interval 1day` (SENSEX) both return real data through the existing client - no new adapter, no new cost model (STT/stamp/GST/SEBI are set by Indian law, not the exchange, so `bse_market()` reuses `EquityCostModel(settings.risk.costs)` verbatim), no new session rules (same 09:15-15:30 IST). New: `config/models.py::BseMarketConfig` + `config/markets/bse.yaml` (optional, defaults match), `markets/market.py::bse_market()`, `broker/indstocks/instruments.py::bse_cash_equities()`. The one real surprise: BSE has no single "EQ" series like NSE - it uses GROUP codes instead (sampled real rows: A/B are genuine actively-traded equities like RELIANCE/NATIONALUM; F is coupon/maturity-coded BONDS; G is GOVERNMENT SECURITIES/sovereign gold bonds - the WRONG ASSET CLASS entirely if included; X/XT/M/MT/T are real but restricted/SME/trade-for-trade subgroups, excluded from v1). `bse_cash_equities()` filters to series A/B only - 2,541 of 12,880 raw BSE "EQUITY" rows. `--market bse` is wired through `data sync-instruments/load/quality/universe/status`, `scan` and `backtest` (own store: `data/bse.duckdb`; own watchlist subfolder `data/watchlists/bse/`, same collision-avoidance reasoning as crypto's). Two real bugs the exact same shape as the NSE VIX bug fixed earlier that day: (1) `CandleStore.index_code()`/`instrument_codes()` both default to `exch="NSE"` - passing BSE's benchmark name without also passing `exch="BSE"` silently resolves to "not found" rather than an error, so every BSE call site now passes `exch="BSE"` explicitly (`_reference_code()` gained an `exch` parameter for this). (2) `instrument_codes()`'s `series` parameter defaults to `"EQ"` (NSE's series) - since BSE rows are stored as series A/B, that default silently returns zero targets for BSE, so BSE call sites now pass `series=None`. `vix_required` stays False and `bse_market.volatility_index` stays `None` (BSE's own duckdb store has no VIX candles loaded into it) rather than half-wiring it, which would reproduce the exact fail-open bug the health check found and fixed for NSE. Verified end-to-end on real data with a small 2-code sample (not the full ~2,500-stock universe, which would be a real, uncommitted-to API/time cost): `data load`, `scan` and `backtest --market bse` all ran cleanly against real BSE candles (RELIANCE, SENSEX). `analysis.py::analyze_symbol` and the dashboard's Lookup tab both support `market=bse` too; its hit-rate/track-record always reports "no history yet" since there is no BSE paper book or call log (deliberately not borrowed from NSE's numbers - different exchange, different stocks). NOT done: syncing/loading BSE's full ~2,500-stock universe (a real, sizeable API-quota decision, left to the user), a BSE-specific universe turnover distribution study (NSE's floor is inherited as a starting guess, unlike crypto's which was data-derived), and BSE paper trading/live monitoring (same NSE-only scope limit as crypto).
- **Historical backfill for crypto/BSE tracking (added 2026-09-12)** - user's follow-up: "can you not take past tracking data for crypto and bse both like we did for nse". NSE's track record exists because the paper book has been running for a while; crypto/BSE's trackers started from zero. Answer: yes, both markets already have (or can cheaply get) years of daily history sitting in their duckdb stores, so a real historical track record can be built in one pass instead of waiting weeks. New `signal_tracker.py::backfill_watchlists(md, cfg, settings, mkt, rows)` replays the EXACT SAME `build_watchlist` step the daily trackers run, once per session across `cfg.start..cfg.end`, against a single `prepare_market` call (features computed once, not per day) - only new signal ids get logged, so it's safe to re-run after the daily job has already logged today's calls. New `scripts/backfill_signal_tracker.py crypto|bse` runs it. Results (both immediately flagged by `flag_setup_failures`, consistent with the existing crypto verdict): **crypto, 2018-2026, 457 resolved calls, all 3 setups underperforming (12-16% hit T1)**; **BSE, 5 large caps 2016-2026, 1,760 resolved calls, all 3 setups underperforming (18-21% hit T1)** - BSE needed its watchlist's history deepened first (`data load ... --years 10`; the existing rows had to be deleted first since `load_history`'s incremental planner takes `max(requested_start, last_stored_bar)` per code, so a code that already has SOME data cannot be backfilled deeper by just asking for more years - this is a real, general limitation of the incremental loader, not new to BSE). Found and fixed a second real bug while running this: `review_queue.py::add_item`'s id was `f"{market}:{timestamp}"` with no dedup - flagging 3 setups in one `flag_setup_failures()` loop happened fast enough that two calls got the identical timestamp, and the second silently overwrote the first in the queue file (only 2 of 3 crypto flags survived on the first backfill run). Fixed with a uuid4 suffix on collision; regression test added (`tests/unit/test_review_queue.py::test_two_items_added_within_the_same_clock_tick_do_not_collide`, frozen-time reproduction). All 7 real review items (1 NSE + 3 crypto + 3 BSE) now sit in `data/reviews/queue.jsonl`, visible and actionable in the dashboard's Review tab today - not something to wait weeks for.
- **Self-analysis extended to crypto and BSE (added 2026-09-12)**, closing the gap flagged right after the review queue shipped ("crypto and BSE have no self-analysis-on-failure wired in at all"). The crypto/BSE tracking machinery (originally all inside `scripts/crypto_signal_tracker.py`) was extracted into `src/tradedesk/signal_tracker.py` - `TrackedSignal`, the JSONL log, `resolve_outcomes` (triple-barrier grading), `scoreboard`/render/dashboard, and a new `flag_setup_failures()` - so `scripts/bse_signal_tracker.py` (new) can reuse it instead of duplicating ~150 lines. `flag_setup_failures()` is deliberately RULE-BASED, not a Claude call: for each setup with >= 5 resolved calls on a market, if its hit rate has dropped below 30% it writes a `review_queue.py` item automatically (title `"<setup> underperforming on <market>"`, one item per market+setup - re-running daily does not spam duplicates). This is the actual "self-analyse if a call fails" answer for crypto and BSE, which have no paper book to run `review week`'s Claude-based analysis against - a dropped hit rate is a fact code can check directly, no LLM or spend-cap risk needed. `scripts/bse_signal_tracker.py` mirrors the crypto script closely (same reason: no BSE paper book either) but is genuinely simpler since it reuses NSE's own broker/credentials - watchlist is 5 liquid Group-A large caps (RELIANCE/HDFCBANK/TCS/INFY/SBIN), benchmark SENSEX, `data/bse_signal_tracking.jsonl`/`data/reports/bse_dashboard.html`/`data/reports/bse_sessions/`. New 6th scheduled task `tradedesk-bse-tracker`, weekdays 16:05 IST (5 min after `tradedesk-after-close`'s data load). **Its queue starts genuinely empty and will show "0 resolved yet" for about the first 10 sessions** (max_hold_sessions) - there is no backlog of BSE calls to analyze, unlike crypto which already had real history. Found and fixed a real bug while wiring this: `claude/weekly_review.py::run_weekly_review` (and the new `flag_setup_failures`) originally wrote into `review_queue.py`'s module-level default path unconditionally - since `tests/claude/test_claude.py` calls `run_weekly_review` with a `tmp_path`, every `uv run pytest` was silently appending a real duplicate row into the PRODUCTION `data/reviews/queue.jsonl`. Fixed by deriving the queue path from the caller's own `out_dir`/`review_path` parameter instead of the module default, with tests added for `flag_setup_failures` proving the fix (`tests/unit/test_signal_tracker.py`) - the polluted production file was manually cleaned up.
- **BSE full-universe live monitoring (added 2026-09-12)**, user explicitly chose "full BSE universe, like NSE" over reusing the 5-stock tracker watchlist, accepting the one-time data-load cost. Verified before building: `live/session.py::ws_code()` (`scrip_code.split("_", 1)` -> `"EXCH:TOKEN"`) is ALREADY fully market-agnostic - `BSE_500325` -> `BSE:500325`, exactly the documented WS format - so `tradedesk live` itself needed no new broker code, only market-aware path defaults. `live()` gained a `--market` option: when not `nse`, the watchlist auto-discovery folder, `--journal` default (`data/<market>_journal.sqlite`) and `--record-dir` default (`data/sessions/<market>/`) all switch automatically, so BSE triggers never land in NSE's `journal.sqlite`/`paper update` population - matching the "never mix markets" discipline used everywhere else in this project. (BSE triggers ARE journaled for the record, but there's no BSE paper book yet to grade them economically - same limitation as `bse_signal_tracker.py`, not new.) Also fixed a latent bug found reading this code path: `run_session`'s tick handler guessed `f"NSE_{t.instrument}"` for an unmapped WS token instead of leaving it unresolved - harmless in practice (every subscribed token is always in the map) but would have silently mislabeled a BSE tick as NSE if that defensive branch were ever actually hit. Two new scheduled tasks (8 total now): `tradedesk-bse-after-close` (weekdays 16:15 IST - `data load --market bse` then `scan --market bse`, builds tomorrow's full-universe BSE watchlist, mirroring `tradedesk-after-close`) and `tradedesk-bse-live-session` (weekdays 09:10-15:35 IST - `tradedesk live --market bse --until 15:35 --no-dashboard`, mirroring `tradedesk-live-session` exactly, same broker/feed/rate-limiter). One-time full historical load (`data load --market bse --years 10`, ~2,542 codes back to 2016) run before scheduling either task, since a scan/live session needs real history to compute indicators from - the daily 100,000-call rate cap made this a non-issue (~2,542/5 ≈ 509 calls, nowhere close). **Same limitation as NSE's own setups (see the M11 rigor-pass entry above): nothing here claims BSE's setups have a real edge** - full-universe monitoring makes BSE alertable in real time, it does not mean the alerts are worth acting on; that question needs its own backtest/track-record the way NSE's and crypto's already got. **Verified end to end 2026-09-12**: full load (4,456,005 bars, 2,542 codes, 0 errors, 6,252/100,000 daily API calls used), then a real `scan --market bse` across all 2,471 codes with candle data found genuine setups (28 entries, all graded C because BSE's regime read risk_off that day - `signals_from_watchlist(alertable_only=True)` correctly finds nothing alertable in that case, so `tradedesk live --market bse` would exit cleanly with "nothing alertable" rather than watch anything, matching NSE's exact behavior in the same regime state). **Known caveat, not fixed**: the scan's shadow ML scoring used the existing NSE-trained model bundle (`latest_bundle()` isn't market-aware) - harmless since `ml.yaml`'s `shadow: true` means it only ever appends an advisory note and never touches a grade or size, but the probability itself is not meaningful for BSE (trained on NSE's feature distributions, e.g. India VIX and Nifty returns BSE's own regime doesn't compute). A BSE-specific model needs its own paper-book/journal history to train against, which doesn't exist yet - out of scope here.
- **Dashboard fixes (2026-09-12), user-reported "columns all look the same, and NSE selection shows crypto"**: (1) real bug in the Lookup tab - switching the market segmented control cleared the symbol input but left the OTHER market's `<datalist>` `<option>`s sitting in the DOM, so clicking into the (now-empty) input could show stale cross-market suggestions before any keystroke fired a fresh `/api/symbols` call. Fixed by clearing `#lookup-options` on every market switch. (2) The Crypto tab's calls table (459 rows after the backfill, only 10 coins/3 setups/5 outcomes) had nothing to narrow it down, so most visible rows looked repetitive. Added three dropdown filters (Coin/Setup/Status) populated from the actual distinct values in the fetched data, client-side filtered - `#crypto-filter-count` shows "N of 459 shown" plus the resolved-calls hit-rate for whatever's currently filtered.
- **Review queue (added 2026-09-12)** - the actual approve/reject surface for "self-learning" the user asked for, which did not exist before this (the earlier answer to "where do I approve" was honestly "nowhere yet"). New `src/tradedesk/review_queue.py`: a small JSONL log (`data/reviews/queue.jsonl`, same pattern as `crypto_signal_tracker.py`'s log) of `ReviewItem`s (title/detail/proposal/status). `tradedesk review week` (`claude/weekly_review.py::run_weekly_review`) now feeds this queue automatically - every "one thing to change next week" Claude names becomes a pending item. Dashboard gained a `Review` tab (`/api/review` GET, `/api/review/decide` POST) with real Approve/Reject buttons. Critically, approving an item ONLY marks it decided - it does not edit any config, setup or threshold. This is deliberate, not a missing feature: CLAUDE.md's hard rule is that Claude's output is advisory text only and can never change a price, level, quantity or signal, and a new setup/threshold change ships only after a human reviews it - so "approve" here means "I've decided to act on this," and actually acting on it (editing risk.yaml, disabling a setup) stays a manual step for the user, exactly as it already was for `review week`'s markdown output before this existed. **Scope: NSE only.** Crypto and BSE have no self-analysis-on-failure wired in at all - crypto's tracker (`crypto_signal_tracker.py`) resolves outcomes but never writes a review-queue item, and BSE has no paper book or journal to analyze failures from in the first place (same NSE-only limitation noted throughout M13). If a "did this call fail and why" flow is wanted for those two markets, that is unbuilt work, not a smaller version of this.
- Dashboard follow-up same day - on-demand lookup + a separate Crypto tab (user wanted to "query and navigate between stocks and coin" while keeping NSE/crypto data independent, and explicitly chose crypto calls stay research/paper-only given the -0.515R verdict, and self-learning stays a human-approves-changes flag/explain loop, not autonomous). New `src/tradedesk/analysis.py::analyze_symbol(settings, market, code, on)` is the ONE shared "what does the agent think of this symbol right now" implementation - both `mcp_server.py`'s `analyze` tool (now takes an optional `market="nse"|"crypto"` param, backward compatible) and the dashboard's new `/api/analyze` call it, so the two surfaces can never disagree. It returns, alongside the existing indicators/patterns/signals: `call` (`"BUY WATCH"` if a setup armed else `"HOLD (no setup armed)"` - deliberately no invented SELL signal for a position you don't hold, per the hard rule that only engine/engine.py::scan_day creates a signal), `hold_days` (the armed signal's `exit_plan.max_hold_sessions`, or the market's config default if nothing armed), and `track_record` (that setup's historical hit-rate, kept PER MARKET on purpose: NSE reads `journal.stats.track_record` off the paper book, crypto reads its own `data/reports/crypto_signal_tracking.jsonl` - the two datasets are never pooled, since a hit rate from one market says nothing about the other's cost structure). `CandleStore.search_codes()` (new) does the "navigate between stocks and coins" part - a case-insensitive code-or-symbol substring search restricted to a market's code prefix, backing both the dashboard's `/api/symbols` autocomplete and any future CLI use. Dashboard gained a `Crypto` tab (reads `/api/crypto/calls`, i.e. that same JSONL log - explicitly labelled "research/paper only, own dataset, not pooled with NSE" in the UI itself) and a `Lookup` tab (market toggle, symbol search-as-you-type, Analyze button rendering the call/hold-days/hit-rate card). Note the user separately asked whether BSE should get the same treatment - it is NOT wired in anywhere (INDstocks/NSE only; no BSE instrument list, corporate actions or codes exist in this codebase) and would be a market-onboarding effort on the scale of M13's crypto work, not a small addition - flagged, not started.
- `tradedesk-after-close` - Mon-Fri 16:00 IST, five actions in sequence: `data load` (incremental, full universe) -> `data load NSE_40000001` (a **safety re-fetch of the NIFTY benchmark specifically** - a real incremental run on 2026-09-12 fetched every equity code's new bar but silently left NIFTY a session stale, root cause not chased down; this re-fetch step exists because a stale benchmark means `scan_config`'s "session after `on`" computation silently targets the wrong date, not a crash) -> `paper update` -> `scan` (builds tomorrow's watchlist for the next `tradedesk-live-session` run) -> `ml check-drift` (added 2026-09-13; flags a review-queue item if the shadow model's calibration has drifted, no-ops cleanly while the paper book has nothing resolved - see the ML training improvement entry below).
  **Real production failure found and fixed, 2026-09-15**: user reported "no calls, no analysis" mid-trading-day on a Tuesday - `data status` showed the whole NSE store frozen at Friday 2026-09-11's close (4 calendar days stale) despite `tradedesk-after-close` reporting `LastTaskResult 0` (success) the evening before. Root cause: this task (and `tradedesk-live-session`, `tradedesk-bse-after-close`, `tradedesk-bse-live-session`) never got the `--no-sync` fix that `tradedesk-crypto-tracker` received on 2026-09-13 for the exact same reason - the permanently-running dashboard task holds a lock on `tradedesk.exe`, and a plain `uv run` (without `--no-sync`) can silently collide with that lock during its own dependency-sync step, exiting 0 without actually running the command. All four tasks now use `--no-sync` on every action. Fixed live, mid-trading-day: ran `data load` (5,268 bars fetched) and `scan` (rebuilt `data/watchlists/2026-09-15.json`) by hand to unblock the rest of that day, confirmed the dashboard's self-refresh picked up the new watchlist within its 60s poll (no restart needed). **The "no calls" itself was correct, separately from the staleness bug**: today's regime read `risk_off` (size multiplier 0.0, no new swing entries) and every entry was Grade C, gated off by the same eligibility policy documented throughout this file (0/500 resolved trades, never beaten a random-timing baseline) - `tradedesk live` correctly exited immediately with "nothing alertable" rather than watching anything. Both things were true at once: the pipeline was broken (stale data, now fixed) AND the "no trade" verdict was the right call given the data once it was fresh.
  **Two follow-up fixes from the same conversation, same day.** (1) **Same-day eligibility pickup**: `tradedesk live` used to just exit the instant nothing was alertable at its 09:10 startup - so even if a human approved a `review_queue.py` item and edited `config/setups.yaml` mid-session, nothing would notice until the next morning's run. New `live/session.py::wait_for_eligible_signals()` (pure, tested with fakes - `tests/live/test_wait_for_eligible.py`) polls a fresh rebuild every `--poll-idle-seconds` (default 300, new CLI flag) until `--until` or something clears the eligibility gate; `cli.py::_live_rebuild_watchlist()` does the minimal core of what `scan` does (scan_config -> prepare_market -> track_records -> build_watchlist), deliberately skipping charts/Claude reads/ML scoring since those are too slow/costly to redo every poll and aren't needed just to re-check eligibility - kept as a separate function from the `scan` CLI command specifically so this can never regress that tested path. This does not weaken the approval gate: it only re-checks the SAME eligibility computation against whatever is already on disk, never lowers a bar, invents a signal, or bypasses a human's config edit. Verified live against real production data (real ~2-3 minute full-universe rebuild each poll, confirmed it correctly found 0 alertable twice in a row and kept running rather than exiting). (2) **The idle-poll now persists every rebuild to disk**, not just the one that finally clears the bar - `dashboard`'s own `--refresh-seconds` reloads on this file's mtime, so without this the Calls tab would keep showing the original 09:10 snapshot for the entire idle-wait instead of each re-check's real numbers.
  **Calls tab was filtering itself to empty, 2026-09-15**: `index.html`'s Today/Calls table only ever showed entries with `rejected_for` empty (i.e. fully on-watchlist) - harmless on a normal day, but on a `risk_off` day where every entry gets rejected, this left the table looking completely blank and indistinguishable from broken (the user's exact report: "I can't see anything in calls tab"). Fixed to show every evaluated entry (matching the pattern Crypto/BSE's calls tables already used) with a new **Tradeable** column (yes/no) and filter dropdown, and the rejection reasons surfaced in the Note column (first reason + "+N more", full list in a hover tooltip) - so a "no trade" day now visibly shows its own reasoning instead of looking empty. Applies to both NSE's `today` and BSE's `bsetoday` tables (shared `renderTodayCalls()`).
  **Nav collapsed from 8 tabs to 4, same day, explicit request.** NSE+BSE now share one "Calls" tab (a `calls-market-select` dropdown picks the market; Lookup - still its own independent NSE/BSE/Crypto segmented control - sits on top of it, replacing the old standalone Lookup tab); Report+Research share one tab (`reportresearch-view-select`); Learning+Review share one tab (`learningreview-view-select`, carrying the review-badge). Each merge is one `<main>` with the two original tab bodies nested inside as `.subtab` divs (same `display:grid` as `main`, no own padding/margin since the parent `<main>` already provides it) toggled by the dropdown instead of a top-level nav click - every inner id, render function and filter was left untouched, so this is a navigation change only, not a UI rewrite. `PERF_CARDS_BY_TAB` became `PERF_CARDS_BY_VIEW`, keyed by tab+sub-view (`calls-nse`/`calls-bse`/`reportresearch-report`/`reportresearch-research`/`crypto`/`learningreview`) via a new `currentView()` helper, since which market's perf-strip card to show now depends on a dropdown, not just which nav button is active. Real bug caught before shipping: the two new wrapper `<main>`s were missing their default `hidden` attribute, which would have shown Report/Research and Learning/Review's content simultaneously with Calls on first page load until any tab was clicked - caught by grep-checking every top-level `<main id="tab-...">`'s default visibility, not assumed correct from the diff.
  **Per-call confidence, 2026-09-15** (explicit request: "so that if we want to take some trade also, we can get the highest percentage confidence trade") - user picked all three options offered rather than one: (1) **Score sort/highlight** - `renderTodayCalls()` now sorts by the existing heuristic score descending and highlights the top-scored row(s) (`.top-score`), purely a display change, no new number invented. (2) **`prediction/predict.py::score_watchlist` now scores REJECTED entries too**, not just `on_watchlist` ones (was `if not e.on_watchlist or pos is None: continue`, now just `if pos is None`) - shadow mode only ever adds a note and enabled mode can only LOWER a grade/size, so this is provably safe on an already-rejected entry (its `alertable` stays False regardless - covered directly, `tests/prediction/test_prediction.py`'s new case checks both shadow and `ON` config). `WatchlistEntry` gained a `probability: float | None` field (set by `apply_probability`, previously only visible buried in a text `score_notes` line) threaded through `dashboard/state.py::set_watchlist` to `/api/state`. (3) **Per-symbol Wilson-lower-bound confidence merged into the Calls table** via the existing `/api/reliability/symbols` endpoint (2026-09-14's work), fetched once per market switch and cached (`symbolConfidenceCache`), not refetched on every SSE push since it changes at most daily. The three numbers - score, model probability, symbol confidence - are shown as three SEPARATE columns, deliberately not blended into one fake composite, since they measure different things and none is currently strong enough to be presented as a single verdict (model ROC-AUC is ~0.5-0.56, coin-flip territory, honestly labelled in the column's hover title). Model retrained same day to un-stale it (was skipping ML scoring entirely, v3 bundle against v4 features) - `tradedesk train --from 2023-09-01 --per-setup`, real numbers: FINAL TEST ROC-AUC base_breakout 0.5396 / nr7_breakout 0.5628 / trend_pullback 0.5202, `has_edge=False` on every setup - consistent with, not contradicting, this project's already-documented finding that these three setups show no real edge. The three new bundles are shadow-only (`ml.yaml: enabled: false`) so nothing here changes what can alert; it only makes the model's (weak) opinion visible per call for a human comparing options manually, exactly as asked.
- `tradedesk-options-snapshot` (added 2026-09-13) - weekdays 13:00 IST, one action: `scripts/options_snapshot.py`. Appends NIFTY's ATM implied volatility and put/call OI ratio to `data/reports/options_snapshot.jsonl`. **Forward-only by necessity** - INDstocks has no historical option-chain endpoint, only a live one, so this data cannot be backfilled and is only useful to a retrain once the log covers a meaningful slice of the training window. Nothing consumes it yet, on purpose (see `market_context()`'s PLANNED-NOT-BUILT note).
- `tradedesk-research-tracker` (added 2026-09-13) - weekdays 16:30 IST (after `tradedesk-after-close`'s data load), one action: `scripts/research_tracker.py run`. **This is the only thing in the project currently generating new evidence, and it exists because of a specific dead end**: the eligibility gate (config/setups.yaml) needs 500 trades / 100 out-of-sample before anything may alert, the paper book is empty, all three setups are blocked as anti-predictive, and every rule that has ever looked good here was SELECTED on 2023-2026 data - so re-testing it on that same data answers nothing. The tracker logs candidate entry rules forward, one session at a time, and grades them later with the same gap-aware `prediction/labeling.py::triple_barrier` used everywhere else.
  Three design choices that are the honest ones rather than the flattering ones: (1) **forward-only, no backfill** - backfilling would replay the window the candidates were chosen on and manufacture a confirmation; the log starts empty on purpose and this is the first genuinely out-of-sample evidence the project has had about these rules. (2) **A random control (`random_eligible`) is logged as a peer rule every single day** - same eligible universe, same dates, same 3xATR/2R/hold-10 geometry, no trigger. Given that the project's single most repeated finding is "the setups lose to random," a null measured live and forward alongside the candidates is the only comparison that cannot be argued away later; it also directly settles the open question `cross_sectional_rsi2.py` raised (does the RSI(2) edge come from the TRIGGER or just from the ELIGIBILITY FILTER - liquid, priced right, above its own ema50?). (3) **Entry is the NEXT session's open, never the arming close** - the rule fires on a close you only know at the close, so levels are stored as an ATR multiple at arming and resolved into real prices from the next bar, the same convention `entry_search.py` used.
  Candidates shipped day one: the control, plus `rsi2_dip_ema50` (the +0.077R/t=7.04 rule), `rsi2_dip_vol` (its best volume-confirmed variant), `rsi2_deep_ema200`, and `pullback_ema10_above_ema50` (fires far more often, so it accumulates evidence fastest). Adding a new idea is one `Candidate(...)` entry in `CANDIDATES` - harness, control, grading and report all come free. That is deliberately the cheap surface for "try a different indicator or a different way of reading the chart."
  Two reporting details worth knowing: the report shows **`green%` (share of trades that ended profitable), NOT the 2R target hit rate** - at a 3xATR stop, touching 2R is a 6-ATR move inside 10 sessions and happens ~3% of the time, so these rules live or die on timeout exits and the target hit rate is a near-constant ~0 (the same reason `mr_model.py` had to train on `label_profit`); and `netR` subtracts a flat 0.128R measured cost drag purely for reporting, which is an approximation, not `risk/costs.py`. Log: `data/reports/research_calls.jsonl`; daily report: `data/reports/research_sessions/<date>.md`. **It cannot alert and is wired to nothing** - it does not touch the watchlist, the paper book, the live scan or the gate.
  **Automatic review flagging, same day**: `flag_research_findings()` runs each candidate's numbers through the EXACT SAME `engine/scoring.py::eligibility()` gate the live setups are held to (not an invented bar) and, if one clears it, writes into `review_queue.py` - never auto-promoted, exactly like every other review-queue item in this project (`signal_tracker.py::flag_setup_failures`'s pattern). Wired into `run`'s nightly job. A synthetic smoke test accidentally wrote a fake finding into the PRODUCTION queue while this was being verified (same mistake class already documented for `weekly_review.py`) - caught by inspecting the file immediately afterward, not assumed safe, and cleaned up before anything shipped.
  **Multi-market generalisation, 2026-09-14** (explicit user request: "the same past and live mechanism for these also... make sure the agent learns throughout by itself" for crypto and BSE too, not just NSE): the module moved from being NSE-only to taking a `market: "nse"|"crypto"|"bse"` parameter throughout (`scan_universe`, `resolve_calls` via explicit db/log args, `rule_stats`, `flag_research_findings`, `build_report`), reusing `markets/market.py`'s existing `Market` bundle (`market_for()`) for each market's own turnover/price floor and - critically for crypto - its stablecoin `exclude_codes`, the same exclusion every other crypto scan in this project already applies. NSE's log path is UNCHANGED (`data/reports/research_calls.jsonl`, already scheduled, already has real data); crypto/BSE get their own market-suffixed files (`research_calls_crypto.jsonl`/`research_calls_bse.jsonl`, `research_sessions_crypto//_bse/`) so the three datasets are never pooled - same discipline as everywhere else in this project. Deliberately reuses the IDENTICAL candidate rules (the RSI(2) family) across all three markets rather than inventing market-specific ones first: whether NSE's measured mean-reversion edge generalises to crypto or BSE at all is itself an open, cheap-to-answer question, and this is the cheapest way to answer it. `flag_research_findings` tags its review item with `[market]` and `market=f"{market}-research"` so the same rule name clearing the bar on two different markets creates two distinct, unambiguous review items. Cost-per-R is now `cost_r_for(market)`: NSE/BSE share 0.128R (same `EquityCostModel`, same STT/stamp/GST/SEBI rules, `markets/market.py::bse_market()` already reuses `EquityCostModel` verbatim); crypto uses a separately-derived 0.16R approximation (M13's measured ~1.47%-of-position round-trip drag, of which the 1% Section 194S TDS is 68%, divided by a typical ~9%-of-price stop distance at 3xATR on a ~3% daily ATR coin - a rough, documented approximation, not a per-trade cost model, same honesty standard as the NSE number). New scheduled tasks: `tradedesk-crypto-research-tracker` (daily 08:10 IST, well clear of the crypto tracker's 30-minute grid) and `tradedesk-bse-research-tracker` (weekdays 16:40 IST, after BSE's own after-close/tracker jobs). Verified with real full-universe first runs on both: crypto seeded real candidates (correctly excluding CDX_USDTINR/USDCINR - the stablecoin exclusion carried through automatically) and BSE seeded its own. Dashboard's Research tab gained a Market dropdown (NSE/Crypto/BSE) driving both `/api/research/summary?market=` and `/api/research/calls?market=`.

  **EOD self-learning step, 2026-09-14** (explicit user request: "build the self learning that reduces [error] over time... even if it means exploring other indicator or chart analysis", initially scoped to NSE/BSE, extended to crypto the same day - see below). New `src/tradedesk/eod_learning.py`, wired as the LAST step of `research_tracker.py run` for every market. What this honestly is, stated up front because it would be easy to oversell: it does NOT adjust anything after every single call, and it touches nothing alertable - `config/setups.yaml`, the live scan and the eligibility gate are all untouched. What it genuinely is: a once-a-day retrain on the REAL, GROWING, FORWARD evidence `research_tracker.py` itself collects - deliberately NOT the 2023-2026 backtest window, which this project has already mined past the point of diminishing, risky returns (the "three independent honest negatives" and the H1/H2 proof plan both already closed that data off from further testing).
  Lifecycle (REWORKED 2026-09-14 - the first version was a one-pass queue of 12 features that stopped learning forever after 12 runs, a fatal flaw in something whose whole job is to keep improving). Each run makes exactly ONE model-affecting decision, in this priority order, so every change is attributable to one measurement and compute stays bounded: **MODEL** (race learner families on pooled OOS Brier, every `MODEL_RACE_EVERY`=12 fits) -> **REVALIDATE** (re-check one already-adopted feature against the LARGER dataset that now exists and DROP it if it no longer earns its place, every `REVALIDATE_EVERY`=8 fits, rotating) -> **EXPLORE** (test the next untried candidate once) -> **CONSOLIDATE** (nothing testable at this sample size; just record the trend). These cycle indefinitely, so the feature set and the learner keep getting re-decided with better evidence instead of being frozen by whatever the first 50 rows said.
  **Why re-testing is not the multiple-testing trap documented all over this file**: the trap is searching for a NEW hypothesis until one prints positive. This is the opposite - the candidate pool is FIXED and finite, and re-tests re-decide an EXISTING question against genuinely more data, with the decision free to go either way. `BRIER_IMPROVEMENT_MARGIN` (0.005) is the bar both to adopt AND to keep, deliberately symmetric so there is no ratchet that only ever adds. A test asserts the constant appears on both sides of `_advance` so a future edit cannot quietly loosen one.
  Two sample-size guards, both doing real work: `_max_features_for(n)` caps active features at ~n/15 (exploration simply PAUSES at the cap - more features on thin data is precisely how you manufacture a good in-sample fit and a worthless out-of-sample one), and `MIN_ROWS_FOR_TREE`=250 keeps xgboost out of the race entirely until the comparison could mean something (a boosted tree over ~30 features on 60 rows is a memorisation machine). Below that floor the loop is not missing a better model - it correctly has one candidate worth fitting. Learner and features both come from `prediction/train.py` (`make_baseline`/`make_xgboost`, both probability-calibrated, xgboost a guarded import) so this module cannot drift from the project's modelling conventions.
  Feature pool: 6 always-on `CORE_FEATURES` plus ~29 candidates grouped into 8 hypothesis families (trend/momentum/volatility/chart_shape/candle_parts/position/volume/event), explored INTERLEAVED one-per-family in rotation rather than draining one family first - the early runs are the only ones that happen before there is much data, so they should sample genuinely different ideas, not six flavours of "price vs a moving average". `chart_shape` is multi-day (opening gap, consecutive-up-close streak, inside/outside day vs yesterday's range). `candle_parts` (added 2026-09-14, explicit request: "add smaller call parts to read graphs to learn it from") decomposes ONE bar into its structural pieces instead of one blunt summary: `body_pct`/`upper_wick_pct`/`lower_wick_pct` exactly partition the day's range (verified directly: a real test proves the three sum to 1.0, not assumed), `body_atr` is the SIGNED body size in volatility-normalised terms, `bullish_candle` is plain candle colour. A long lower wick and a long upper wick are opposite tells even on a day with an identical `close_range_pos` (kept in this family, not chart_shape), and nothing before this fed that distinction to a model. None of chart_shape/candle_parts is used anywhere else in this project. The `event` family (results/corporate-action proximity) is real data already in the store that no model here had ever been given, harvested from the deleted mr_model.py; `near_term_results` is capped at 4 CALENDAR days because `results_events` stores only the meeting date, never when it was ANNOUNCED, and SEBI LODR requires just ~2 working days' notice - an uncapped countdown would claim knowledge that was plausibly not yet public. Crypto drops the event family outright (no earnings calendar for a coin - those columns would be a constant, not a signal).
  **DuckDB lock retry, 2026-09-14** (found for real, not theorised: `tradedesk-crypto-research-tracker`'s scheduled run failed with exit code 1 on 2026-09-14 - manually reproducing the exact command succeeded, pointing at a transient cause; crypto's data-load task runs every 30 minutes and DuckDB is single-writer/single-reader per file, so a collision was plausible). `CandleStore.__init__` now retries through `duckdb.IOException` with exponential backoff (1/2/4/8/16s, ~31s worst case) before raising, via a single choke point (`_connect_with_retry`) that protects every caller project-wide, not just the research/learning scripts. Proven with GENUINE cross-process locks (a real subprocess holding the file open), not mocked - same-process double-connects were checked first and do NOT reproduce DuckDB's real file lock at all (confirmed by hand: `duckdb.connect()` twice in one process succeeds cleanly), so a test using that would have proven nothing. `tests/data/test_candle_store.py`'s two new tests hold a real lock via a subprocess, confirm the retry actually waits (elapsed time > 0.1s, not a race that happened to pass) and recovers once the subprocess releases it (~3.2s in manual verification), and separately confirm an unreleased lock exhausts retries and raises the real `duckdb.IOException` rather than hanging forever (~33s in manual verification, matching the backoff schedule exactly).
  **Runs for ALL THREE markets** (NSE/BSE once daily bundled into their nightly `research_tracker run`; crypto TWICE daily since its data is 24/7 - the second pass is the standalone `research_tracker.py eod-learn --market crypto` on its own 20:10 IST task). Each market keeps its own state (`data/models/eod_learning_state_<market>.json`) and its own append-only history (`eod_learning_<market>.jsonl`, one row per run carrying phase, model_kind, n, Brier, ROC-AUC, net R, active feature count and a plain-English `summary` of what that run decided) - never pooled. `LearningState.load` still reads the pre-rework format so no history was orphaned. Dashboard's Learning tab renders that history with the `summary` column as the main thing to watch; Brier is the error being driven down, read as a trend across rows, not judged on any single row.
  Verified by `tests/unit/test_eod_learning.py` (9 tests) driving `_advance` against a CRAFTED dataset with a planted predictive column and pure noise elsewhere, so the tests prove the loop reaches the RIGHT decision rather than merely running: it rejected 6 noise features, adopted the planted one (Brier 0.2514 -> 0.1200), then kept it on revalidation (removing it would cost +0.1283 Brier); a force-adopted noise feature is correctly DROPPED on its revalidation pass. Also covered: the feature cap, crypto's event exclusion, family interleaving, legacy state migration, and the symmetric-margin guard. Against the real (still near-empty) logs all three markets correctly report "not enough data (0/50 resolved)" rather than fitting noise.
  **Same gate, same "flag never apply" rule as everywhere else.** If the model ever clears `engine/scoring.py::eligibility()`'s default policy AND ROC-AUC>=0.55 AND |t|>=2 on the locked tail, it writes one `review_queue.py` item (`market=f"{market}-eod-learning"`) - a human decision, never an automatic promotion. `test_net_r` is a REAL net figure (gross minus `research_tracker.cost_r_for(market)`); an earlier version reported gross under a net label, harmless-ish for NSE/BSE (0.128R) but would have understated crypto's heavier 0.16R drag first.
  **Why crypto matters most for the chart_shape family** (the user's own reasoning when asking for crypto to be included): crypto has no fundamentals, no VIX and no regime context the way NSE/BSE do, so its setups genuinely are closer to pure chart-pattern reading - which makes it the natural place to find out whether the chart-derived exploration features actually help, and the reason the loop must cover all three markets rather than NSE alone.
- `tradedesk-crypto-tracker` - **3x daily as of 2026-09-12 (07:00/13:00/19:00 IST, ~6h apart)**, per user request; still TWO actions each run: `data load --market crypto` (full 338-pair refresh) then `scripts/crypto_signal_tracker.py`. The load step was added 2026-09-12: the tracker script only refreshes its own 10-pair WATCHLIST, so the other 328 pairs sat a day stale permanently and any full-universe crypto scan/backtest silently used yesterday's data. Answers "test a few coins and track if you're right" without needing crypto paper trading: logs every signal the crypto scan detects on a fixed 10-pair watchlist (BTC/ETH/SOL/XRP/DOGE/ADA/TRX/XLM/HBAR/BNB, `WATCHLIST` in the script), including ones rejected on net R:R (the old blanket "size 0" rejections are gone since fractional sizing landed), then grades each one later with the exact triple-barrier rule prediction/labeling.py trains on (hit T1 before the stop, or not) once enough days have passed. Deliberately does NOT filter to only "tradeable" signals first run showed every BTC/ETH/BNB signal rejected purely on "size 0" (the same whole-unit-sizing limitation above), which is a money question, not a "was the pattern right" question; `rejected_for` stays on each logged row so the two are visible separately. Log: `data/reports/crypto_signal_tracking.jsonl`. Verified against real historical signals before scheduling (6 real nr7_breakout signals from 2026-09-04 to 09-06, all resolved via triple_barrier, 2/6 hit T1). **On the 3x/day change**: crypto's setups are daily-bar only and CoinDCX's daily candle finalises once (~05:30 IST), so a 13:00/19:00 run typically logs 0 NEW signals - that's expected, not a miss, and `signal_tracker.py::render_session_report`'s new `run_at` param says so explicitly in the report rather than leaving it to look like nothing happened. The real value of running 3x is re-running `resolve_outcomes`/`flag_setup_failures` more often (fresher hit-rate/Review-tab flags through the day) and redundancy against a slow/failed morning API call, not more distinct calls. `save_session_report(..., append=True)` now appends each run's report to the day's file instead of overwriting it, so `data/reports/crypto_sessions/<date>.md` shows all of a day's checks, separated by `---`.

  **Every 30 minutes as of 2026-09-13** (superseding the 3x/day cadence above), per explicit user request for closer monitoring - same two actions, both now run with `--no-sync` (the permanently-running dashboard task holds a lock on `tradedesk.exe` that `uv run`'s normal dependency-sync step can collide with; a 30-minute cadence makes that collision far more likely to actually happen than 3x/day did, so this was fixed proactively rather than waiting to see it fail). Registered via `New-ScheduledTaskTrigger -Once -RepetitionInterval 30min -RepetitionDuration 3650 days` (Windows Task Scheduler's PowerShell module has no direct "repeat forever" - a 10-year repetition window is the standard practical substitute). The "0 new signals most runs" caveat above applies even more at this cadence - most runs still just re-resolve/re-flag, which is the actual value of running this often, not new distinct calls.

  **Live vs Past tagging, same day**, per explicit user request ("put past calls under a different tag, live calls under a different tag... easy to monitor"): `TrackedSignal` (signal_tracker.py) gained a `source: "live" | "backfill"` field, defaulting to `"backfill"` - so every row logged before this field existed (the entire 2018-2026 one-shot replay, and any live-tracker row logged before 2026-09-13) resolves to `"backfill"` on the JSONL round trip with NO migration needed, since `TrackedSignal(**r)` falls back to the dataclass default for a key an old row doesn't have (and the dashboard's raw-JSON reader, `_read_call_log`, does the identical `setdefault("source", "backfill")` so both paths agree). `log_new_signals(..., source="live")` defaults to `"live"` so the normal daily tracker scripts (crypto/BSE) needed no call-site change; `backfill_watchlists()` passes `source="backfill"` explicitly. Verified against the real production log: a real run logged one new SOLINR trend_pullback signal, correctly tagged `"live"`, alongside the existing 461 backfilled rows all correctly reading `"backfill"`.
  Dashboard: the Crypto tab is now a segmented Live/Past view (mirroring BSE's existing live/history segment) rather than one combined table - `crypto-live` and `crypto-past` are two client-side filters over one `/api/crypto/calls` fetch (split by `source`), reusing the existing generic `renderCalls()`/`populateCallsFilters()` functions unchanged (they were already parameterized by a "market" string used to build element ids, so `crypto-live`/`crypto-past` slot in without new rendering code). The Live view adds a pass/fail scoreboard (resolved / hit rate / avg P/L / still open) computed only from `source === "live"` rows - this is "the pass/fail of the current agent's calls," refined into its own view exactly as asked, kept separate from the large historical backfill's own numbers so the two can't be averaged together by accident. BSE was NOT split the same way in this first pass (the ask was specifically scoped to crypto that day); the `source` field existed on BSE's rows too via the shared dataclass, and the same split was added there the next day (2026-09-14) - see below.

  **Refined further, 2026-09-14** (explicit user feedback: past belongs under its own dropdown, not a peer toggle, and "ongoing" vs "pass/fail" are different questions): the segmented Live/Past buttons became two dropdowns - "Data" (Live/Past) and, shown only when Live is selected, "View" (Ongoing/Pass-Fail). Live itself split into `crypto-ongoing` (unresolved) and `crypto-passfail` (resolved - the actual scoreboard), still all client-side filters over the same one `/api/crypto/calls` fetch. Generalised into shared `showLiveView(prefix, view)`/`wireLiveSplitDropdowns(prefix, ...)`/`renderLiveScoreboard(prefix)` functions and applied to **BSE's existing "Call history" sub-view identically** (`bse-ongoing`/`bse-passfail`/`bse-past`, over `/api/bse/calls`) - both markets now share one implementation, parameterized only by an id prefix.

  **Backfill/confidence/reliability rework, 2026-09-14** (explicit user request: "remove backfill altogether from UI and just add pl % in a timeframe for backfill and performance confidence % on stocks or coin by agent and one whole agent confidence or reliability % on top right which can be monitored by me that it's increasing over learning or not"). Four parts. (1) **Removed the row-by-row "Past (backfill)" table** from both Crypto and BSE (`crypto-view-past`/`bse-hist-past`, the `crypto-data-select`/`bse-data-select` Live/Past dropdowns, and `wireLiveSplitDropdowns` - the Live split (Ongoing/Pass-Fail) stays, just no longer paired with a Data toggle since there's only one data source left in the browsable tables). (2) **New `src/tradedesk/reliability.py`**: `wilson_lower_bound(wins, n)` - the Wilson score interval's LOWER bound, not raw win rate, so a coin with 1 resolved call and a win reads LOW rather than "100% reliable" (this project has repeatedly caught and corrected exactly that overclaiming shape - base_breakout's "sweet spot", the 150-code cross-sectional smoke test). Hand-rolled, not `statsmodels` (not installed; this machine's Smart App Control has already blocked other unsigned-DLL wheels), matching the project's existing hand-written-indicators pattern. Verified by structural/property tests (`tests/unit/test_reliability.py`) since no memorised reference constant is checkable offline: monotonic in wins, monotonic in n at a fixed rate, always in [0,1], never exceeds the raw win rate, converges to the true rate as n grows, and a 60/100 track record correctly outranks a 1/1 "lucky streak". (3) **New `src/tradedesk/reliability_sources.py`** turns each market's own log into the shapes reliability.py's stats take: `crypto_bse_symbol_confidence`/`nse_symbol_confidence` (per-symbol Wilson lower bound), `crypto_bse_backfill_pnl` (n/win-rate/avg-R/pnl_pct over a selectable timeframe - r_multiple summed and scaled by risk.yaml's own 0.25% per-trade risk assumption, since backfilled calls were never actually sized against real capital; documented as an approximation, same honesty standard as `research_tracker.cost_r_for`), and `overall_reliability_now()`/`log_daily_reliability_snapshot()` for the pooled top-line number - scoped to REAL LIVE resolved calls only (NSE paper book + crypto/BSE `source="live"`), excluding backfill (real history, not something "the agent" did) and research_tracker's forward candidates (can't alert, don't represent an actionable call) by the same reasoning `overall_reliability`'s own docstring states. Tested against real fixtures (a real Journal/paper_trades table, real JSONL rows) rather than properties, since this module is I/O glue, not math - `tests/unit/test_reliability_sources.py`. (4) **Dashboard**: `/api/reliability/overall`, `/api/reliability/history`, `/api/reliability/symbols?market=`, `/api/reliability/backfill-pnl?market=&days=` (new endpoints, `tests/alerts/test_reliability_endpoints.py`); Crypto/BSE tabs gained a "Backfill P&L" card (timeframe dropdown: 30d/90d/1y/all) and a "Coin/Stock confidence" table where the past-browsing table used to be; the header gained a top-right `agent reliability <pct> (n=<n>) <trend arrow>` indicator reading `/api/reliability/overall` + `/api/reliability/history` (comparing today's logged snapshot to the previous one for the arrow). The daily snapshot write is hooked into NSE's `research_tracker.py run` specifically (fires exactly once/day at 16:30 IST, unlike crypto's twice-daily or BSE's differently-timed run) via `log_daily_reliability_snapshot()`, which is itself idempotent within a day (checks the history for today's date before writing) as a second layer of safety. **Verified live in production, not just in tests**: restarting `tradedesk-dashboard` picked up the new routes immediately (real numbers: crypto backfill 461 resolved calls at -53.5% pnl_pct/14.8% win rate, BSE 1,760 at -157.2%/19.9% - both consistent with this project's already-documented anti-predictive setup findings), and the 16:30 IST `tradedesk-research-tracker` task's real production run independently exercised the new hook and wrote the first genuine `data/reports/agent_reliability_history.jsonl` row without being asked to.

## Hard rules
- NEVER call or implement order placement, modification or cancellation unless the task explicitly says "Milestone M12".
- The API reference is docs/indstocks-api.md. Never guess endpoints, fields, formats or limits; read the doc.
- Every INDstocks call goes through broker/indstocks/ratelimit.py. Never exceed documented limits.
- NEVER hardcode tokens or the TOTP secret. Tokens live in memory; the TOTP secret comes from keyring.
- The live scanner and the backtester share engine/engine.py. No live-only or backtest-only signal logic.
- Fills are gap-aware everywhere: an open beyond a stop or trigger fills at the open.
- Overnight positions are long-only.
- No feature, signal or label may use data from after its timestamp. Every setup and feature has a look-ahead or leakage test.
- Portfolio limits (heat, positions, sector caps, daily and weekly limits) apply in backtests, paper book and live alike.
- Fail closed: stale data, a disconnected feed or API errors pause alerts and raise a health alert.
- Never block the asyncio event loop; CPU-heavy work runs in a worker thread.
- Timezone is Asia/Kolkata. Candle logic uses exchange timestamps, not the local clock.
- Claude output is advisory text only; it can never create a signal or change a price, level or quantity.
- The prediction layer can only lower grades or size, never raise size beyond config caps.
- Changes under engine/, setups/, live/ or data/ must pass tests/replay and tests/gaps.

## Commands
- Tests: uv run pytest
- Lint and types: uv run ruff check . && uv run mypy src
- Config check: uv run tradedesk config check
- Cost breakdown: uv run tradedesk costs --type delivery --qty 20 --entry 842 --exit 890
- Credentials: uv run tradedesk auth setup [--stdin] | auth status | auth check | auth clear   (keychain only; hidden input only works in a real Windows console, not Git Bash/mintty)
- Instruments: uv run tradedesk instruments refresh
- Candles / quote / stream: uv run tradedesk candles NSE_3045 --interval 1day --days 30 | quote NSE_3045 | stream NSE:3045 --seconds 30
- Data store: uv run tradedesk data sync-instruments | data load [--interval 1day] | data import-actions <nse.csv> | data import-results <nse.csv> | data quality [--out data/reports/q.csv] | data universe [--on YYYY-MM-DD] | data status
- Crypto data (M13, CoinDCX, no credentials needed): same data subcommands with `--market crypto` -> data/crypto.duckdb by default; scrip codes are `CDX_<coindcx_name>` e.g. CDX_BTCINR
- Live session: uv run tradedesk live [--watchlist data/watchlists/<date>.json] [--until 15:35] [--market nse|bse]   -> records data/sessions/<date>.jsonl (or data/sessions/<market>/<date>.jsonl, own journal, for bse/crypto)
- Replay: uv run tradedesk replay data/sessions/<date>.jsonl --watchlist data/watchlists/<date>.json [--alerts]
- Claude: uv run tradedesk scan --claude notify|veto ; uv run tradedesk review week ; uv run tradedesk mcp (also .mcp.json + .claude/skills analyze|review|backtest)
- Journal: uv run tradedesk journal fill <signal_id> --qty --price | journal exit <id> --qty --price --reason | journal stop <id> --to | journal positions | journal tag <id> rule_break "note" | journal stats | journal orders
- Paper book: uv run tradedesk paper update [--date]   (run after the close and `data load`)
- Alerts: uv run tradedesk alerts setup-telegram | alerts telegram-chat-id | alerts test ; dashboard: uv run tradedesk dashboard [--watchlist ...] [--session ...] [--journal data/journal.sqlite] (live embeds it by default at 127.0.0.1:8765; Today tab is the live view, Performance tab is EOD + weekly/monthly from the paper book)
- Evening scan: uv run tradedesk scan [--date YYYY-MM-DD] [--setup base_breakout] [--charts]   -> data/watchlists/<date>.json (+ PNGs)
- Backtest: uv run tradedesk backtest --setup base_breakout --from 2023-09-01 [--to ...] [--split 2025-09-01] [--out data/reports/trades.csv]
- Train model (shadow): uv run tradedesk train --from 2023-09-01 [--to ...] [--setup base_breakout] [--splits 4] [--dataset data/reports/ds.csv] [--final-test-frac 0.2] [--auto-threshold/--fixed-threshold]   -> data/models/<version>.joblib (+ .json), prints validation vs FINAL TEST metrics separately, a per-setup breakdown, and a Strategy A vs C (existing rules vs rules+ML) economic comparison; `scan` then logs shadow probabilities to data/models/shadow.jsonl. Each model family grid-searches a small hyperparameter set inside the walk-forward dev folds (never the locked final test set), so a run is slower than it used to be but the saved `hyperparameters` are the ones actually chosen.
- Drift check: uv run tradedesk ml check-drift [--shadow-log data/models/shadow.jsonl] [--review-path data/reviews/queue.jsonl]   (also runs as the 5th step of tradedesk-after-close; flags a review-queue item, never edits config/ml.yaml)

## Conventions
- Money and rates are `Decimal`, never float. Config percentages are fractions (0.001 = 0.1%).
- Cost lines are rounded half-up to the paisa individually before summing; STT and stamp duty are rounded to the nearest RUPEE per trade (ledger-verified: Rs 0.86 STT -> 1, Rs 0.13 stamp -> 0). GST = 18% x (brokerage + exchange txn + SEBI) - not IPFT/STT/stamp. Brokerage = 0.1% capped at Rs 5 with a Rs 2 minimum (Muhurat reversal 2.36 = 2 + GST).
- Net R:R = (gross reward − costs to target) / (gross risk + costs to stop). A stop-out loses more than 1R. PLAN.md §7's worked numbers (1.4R/2.3R, ~₹110) assume ₹20/order; INDmoney actually charges 0.1% capped at ₹5 (min ₹2) per order, so the same card is ₹72 and 1.61R/2.48R.
- The cost calculator never infers intraday vs delivery; the caller passes `TradeType`. Same-day buy+sell is INTRADAY.
- Charge rates live in config/risk.yaml under `costs:`. Real contract-note figures live in tests/golden/contract_notes.yaml; if INDmoney changes pricing, update both.
- tests/golden/rate_card_examples.yaml + test_rate_card.py are a PROVISIONAL stand-in for the missing intraday golden notes: hand-computed from the documented rate card, not a real bill. They are never the M1 done-when count (test_golden_coverage_report reads contract_notes.yaml only) - delete rate_card_examples.yaml once a real intraday note lands there.
- INDstocks facts baked into broker/indstocks/: REST codes are `NSE_3045`, WebSocket codes `NSE:3045`; candle `ts` is the OPEN time in epoch seconds, requests use epoch ms; ≤5 codes per candle call, ≤1000 per quote call, ≤3000 instruments per WS connection; one TOTP token live at a time (24 h, 1 generation/min).
- The doc's FAQ section contradicts the endpoint pages (different WS URL, `symbols` param). Follow the endpoint pages and the OpenAPI spec, never the FAQ.
- Market-data prices are float (pandas/DuckDB bound); accounting money is Decimal.
- Indicators (engine/indicators.py) are hand-written pandas with TradingView conventions (ema seeded on first value; rma = Wilder, SMA-seeded, na until n finite values; population stdev). No TA-Lib: its C DLL would hit Smart App Control.
- Pattern detectors (engine/patterns.py) evaluate the LAST bar of the frame they receive and return geometry models; look-ahead freedom comes from slicing the frame to the evaluation date. Pivots carry `confirmed_at`.
- tests/engine/test_indicators.py::test_daily_features_have_no_look_ahead is the leakage test for every feature column; extend `daily_features` and it is covered automatically.
- engine/engine.py::scan_day is THE daily scan; engine/intraday_engine.py::scan_bar (2026-09-13, SDD intraday plan) is its intraday sibling. Signals are created ONLY by these two functions - never add signal logic anywhere else. The backtester (backtest/runner.py) calls scan_day with frames sliced to each session; the live evening scan (M6) calls it with today's frames. scan_bar takes an IntradaySnapshot (one timeframe, one bar-close `at`) and slices every frame to the last CLOSED bar itself via engine/mtf.py::last_closed_bar (ts + interval.seconds <= at) before any setup ever sees it - the same safety-net philosophy as scan_day's slice_to, proven by a test that plants wildly different future rows in the frame and confirms the armed signal is byte-identical whether or not they are physically present.
- Entry confirmation is live/confirmation.py::confirm_trigger (15-minute CLOSE above the level; the 09:15 bar never triggers; bars starting >= 15:00 defer to the daily close; optional volume-vs-slot-norm). The live monitor and the backtester both call it; the backtester uses it whenever 15-minute candles are in the store, else falls back to daily bars (open beyond trigger+1 ATR = chased; open past the stop = out at the open; a bar touching both stop and target = stop). Exits are daily-bar in the backtester either way.
- live/trigger_monitor.py fails closed: no ticks for 120 s pauses alerts (DATA STALE); alerts raised while paused are delivered marked [delayed] on resume; after a WebSocket reconnect `resync()` checks triggers/stops against REST day high/low before alerts resume.
- tests/live/test_live.py::test_replay_matches_backtester_triggers is the M7 parity test (same 15-minute bars as ticks -> same triggered ids and fill prices).
- Alerts (M8): alerts/router.py routes by the entry's grade (A -> desktop+Telegram+dashboard, B -> dashboard, C -> log only) or by level for non-signal alerts; Telegram sends are queued and drained by AlertRouter.worker so the monitor never blocks. Desktop = PowerShell/WinRT toast + winsound (no packages). Telegram = raw Bot API over httpx; token in keychain `tradedesk-telegram`; only alerts.yaml `allowed_chat_id` is answered. Dashboard = FastAPI + SSE on 127.0.0.1 only; DashboardState.publish() pushes every change. Two tabs (2026-09-12): Today (the SSE-driven live view above, unchanged) and Performance - EOD (date picker, defaults today) and weekly/monthly rollups, both read straight from the paper book via new journal/stats.py::eod_report/performance_rollup (pure reporting, no new signal/sizing logic) through new /api/eod and /api/performance endpoints. `create_app(state, journal_path=...)` - journal_path is optional so the existing tests/alerts call site (`create_app(state)`) is untouched; without it those two endpoints 404 rather than crash. `tradedesk dashboard` run standalone (no `tradedesk live` active) also replays today's data/sessions/<date>.jsonl if one exists, through a fresh TriggerMonitor with no alert routing, so "signals today" shows what actually triggered, not just the plan - same engine tests/live's parity test already covers. Found while wiring this: `tradedesk scan --market crypto` saved into the same flat data/watchlists/ folder as NSE (filenames are date-only), so `dashboard`/`live`'s market-blind "newest file" auto-discovery could silently load a crypto watchlist as NSE's - crypto now saves under data/watchlists/crypto/.
- Telegram "Took it"/"Skip" -> live/session.py::apply_decision moves TRIGGERED -> TAKEN/SKIPPED (the journal records it in M9).
- httpx's ASGITransport buffers responses: test SSE against a real uvicorn server on a free port (see tests/alerts).
- Journal (M9) is SQLite at data/journal.sqlite (journal/db.py). Real fills are entered by hand (`journal fill/exit`) or via Telegram Took it/Skip; the broker order-updates feed is only captured raw (`order_updates` table) because its messages carry no instrument - matching to signals is manual until an order-book endpoint is wired.
- Paper book (paper/book.py) simulates EVERY triggered signal with the live exit rules and full costs, sized per risk.yaml against its own equity, deliberately WITHOUT portfolio crowding limits (it measures setups, not queue order). `journal.stats.track_record` -> TrackRecord -> scoring; a setup with a negative rolling-30 paper expectancy is benched (never alerts).
- Claude (M10) lives in claude/: output schemas in claude/models.py are TEXT ONLY (tests assert no int/float field exists); claude/chart_read.py::apply_reads is the only place a read touches the watchlist and it can only add a note, lower a grade or reject an entry - never a price, level, quantity or score. Trigger notes are queued by TriggerNoteWorker.on_alert and produced in a background task AFTER the alert is out. The SDK is called via client.messages.parse(output_format=<pydantic>); credentials resolve from ANTHROPIC_API_KEY / `ant auth login`; spend is logged to data/claude_usage.jsonl and capped by claude.yaml monthly_spend_limit_usd. Models are those PLAN.md names (sonnet-5 for reads/review, haiku-4-5 for notes); change in config/claude.yaml.
- mcp_server.py exposes read-only tools (get_watchlist, get_position, analyze, journal_stats, backtest) via the mcp 2.x MCPServer API (`from mcp.server.mcpserver import MCPServer`; FastMCP no longer exists).
- risk/limits.py::RiskManager rebuilds the Portfolio counters from journaled live trades in exit order (weekly start equity, consecutive-loss pause, re-entry cooldowns) so restarts are deterministic; it adds the results blackout on top of Portfolio.can_enter.
- Signal lifecycle is engine/lifecycle.py (ARMED -> TRIGGERED/CHASED/EXPIRED/INVALIDATED -> TAKEN/SKIPPED -> OPEN -> CLOSED); illegal transitions raise.
- Evening scan = backtest/runner.py::prepare_market + build_snapshot + engine.scan_day, then engine/scoring.py (grade), risk/sizing.py, engine/filters.py. tests/scan proves watchlist signals for a date == the backtester's for that date (given the same exclusion set).
- Scoring weights are in engine/scoring.py::WEIGHTS; TrackRecord (rolling paper expectancy, benched flag) is supplied by M9 and neutral until 30 trades.
- Charts: alerts/charts.py renders mplfinance PNGs headless (Agg); geometry bar indices refer to the FULL feature frame, so pass the unsliced frame up to the arming session.
- Portfolio limits live in backtest/portfolio.py::Portfolio.can_enter and are the same object the paper book and live risk manager will use (M9).
- Backtest e2e fixtures live in tests/backtest/synth_universe.py; the ATR stop rule (2x) is deliberately relaxed to 3x there because ATR decays inside a synthetic tight base.
- The INDstocks feed ALREADY serves split/bonus-adjusted daily history (verified 2026-09-12 against 40 real NSE corporate actions: 39 smooth across the ex-date). So `CandleStore` defaults to `apply_corporate_actions=False` and `load(adjusted=True)` does NOT rescale. Applying factors on top double-adjusts and silently corrupts history (NESTLEIND's 1:10 split turned Rs 1,361 into Rs 68). The corporate_actions table is still imported - it drives the `suspected_unadjusted_split` detector and audit - just not applied. Only pass `apply_corporate_actions=True` for a feed that genuinely serves unadjusted prices. Never write adjusted prices back.
- `suspected_unadjusted_split` in the quality report is a RATIO HEURISTIC, not evidence: it matches any close/prev_close near a known split/bonus factor, so rights issues and genuine crashes trip it (e.g. BALKRISHNA 2024-03-07 was a rights issue at a deep discount, not a bonus). Always confirm against the corporate_actions table before acting on one.
- The trading calendar is the benchmark index's daily candle dates (config/universe.yaml `benchmark`); universe membership is computed as-of-date from stored candles (survivorship caveat stays in every backtest report).
- Windows Smart App Control is ON on this machine: it blocks unsigned DLLs in brand-new wheels (numpy 2.5 / pandas 3 failed). Pins in pyproject.toml exist for that reason; if a new package fails with "Application Control policy has blocked this file", pin an older, widely-distributed version.
- Prediction layer (M11) lives in prediction/: labeling.py (triple barrier: T1 -> 1, stop or gap-through-stop -> 0, vertical barrier max_hold -> 0; a bar touching both = stop, as in the backtester), features.py (33 features as of 2026-09-12 - see below - all known at the arming close; tests assert a truncated frame gives identical features), train.py (build_dataset from a BacktestResult, purged walk-forward BY DATE with ml.yaml embargo_sessions, sigmoid-calibrated logistic baseline; LightGBM only if importable AND better OOS Brier - it is NOT installed here, Smart App Control), predict.py (apply_probability can only LOWER a grade or halve qty, gated by ml.yaml enabled+not shadow; shadow mode only appends a score note and logs), calibration.py (drift_check pauses the layer when a bucket with >= 30 resolved signals drifts > 0.15). `tradedesk train` refuses --no-shadow; switching on is a config edit, never a CLI flag.

  **M11 rigor pass (2026-09-12)**, from a user-supplied "ML Prediction Layer Improvement" PDF: an Explore-agent audit found most of the PDF's asks (baseline+challenger, purged walk-forward, shadow-mode, leakage tests) already built, so this pass targeted only the verified gaps. (1) **A genuinely locked final test set** - `train()` now reserves the most recent `final_test_frac` (default 20%) of history BEFORE walk-forward folds ever see it; model AND threshold selection happen only on the earlier "dev" data, the reserved block is scored exactly once with the already-chosen model/threshold (`TrainReport.final_test`) - previously the same walk-forward OOS predictions fed both model selection and the reported metric, which is a real (if subtle) form of leakage into the "out of sample" number. (2) **Full ML metrics** - `evaluate()` now also computes ROC-AUC/PR-AUC/precision/recall/F1/log-loss (degrading to `None` on a single-class fold rather than raising). (3) **Reproducibility** - every estimator now takes a `seed` (default `DEFAULT_SEED = 20260101`), including `CalibratedClassifierCV`'s internal `StratifiedKFold`. (4) **Complete artifacts** - the `.json` sidecar now carries `training_data_range`, `feature_version` (`features.py::FEATURE_VERSION`, bumped to "v2"), `target_definition`, `hyperparameters`, `random_seed`, `feature_list` (previously joblib-only), `final_test`, `threshold_grid` and the top-10 `feature_importance` - previously only 6 of the PDF's 10 requested fields existed and none were tested; `tests/prediction/test_prediction.py::test_artifact_json_has_every_field_the_bundle_promises` closes that gap. (5) **`ModelBundle.feature_importance()`** - kind-agnostic (logistic coefficients by magnitude, or `.feature_importances_` for a tree model), where the old `coefficients()` only worked for logistic and silently returned `None` otherwise. (6) **XGBoost added and it WORKS here** - unlike LightGBM, `uv add xgboost` (3.4.1) both installs AND imports cleanly, no Smart App Control block; `make_xgboost()` mirrors `make_lightgbm()`'s exact guarded-import pattern and competes in the same OOS-Brier race. (7) **`prediction/train.py::compare_strategies()`** - the actual economic question the PDF's Strategy A/B/C section asks for: joins the trained bundle's probability to each closed trade via `t.position.signal.id` (the same join `build_dataset` already uses) and reports `backtest/reports.py`'s `metrics()`/`max_drawdown()` plus two NEW functions, `sharpe_ratio()` and `cagr()` (there was previously no Sharpe or CAGR ANYWHERE in this repo), for "all triggered signals" vs "probability >= threshold" side by side. Strategy B (ML-only, no rule gate) is explicitly out of scope - nothing but `engine/engine.py::scan_day` may create a signal. (8) `select_threshold()` grid-searches 0.50-0.80 on VALIDATION-fold predictions only (never the final test set) and reports a `has_edge` flag. (9) Two new features, `nifty_return_1d/5d` (`market_context()` in train.py, sourced from `md.benchmark` - already loaded for the regime calc, no new data pipeline) - `sector_return_1d/5d` was deliberately NOT added: `config/universe.yaml`'s `sector_indices` list (9 real NSE sector indices) turned out to be completely unused anywhere in the codebase, and `sector_percentile` (the existing sector feature) is ALSO never actually populated by any real caller (always defaults to 50.0) - building sector_return properly needs a stock-to-sector-index mapping and sector candle loading that doesn't exist yet, a real follow-up, not a two-line addition.

  **ML training improvement, Phase 1+2 (2026-09-13)**, following a user request to plan "the best solution for the training model" - a 3-agent exploration pass (feature/model/validation state, CLI/reports state, data-source landscape) confirmed the M11 rigor pass's own honest finding (OOS ROC-AUC 0.556, `has_edge=False`) meant methodology gaps, not missing data, were the higher-priority fix; user chose methodology first, then real new data sources, in that order (plan: `C:\Users\sibap\.claude\plans\c-users-sibap-claude-uploads-121e0231-c-sequential-mango.md`).

  **Phase 0 (data acquisition, done first per explicit user instruction)**: (1) Curated `config/sector_membership.yaml` from NSE's own published index-constituent CSVs (niftyindices.com, fetched live) - originally attempted all 9 `config/universe.yaml::sector_indices`, but **verified against the live INDstocks historical-candle API that only 2 of the 9 (BANK NIFTY, Nifty Financial) actually return history** - the other 7 (IT/Auto/Pharma/FMCG/Metal/Realty/Energy) resolve to a real scrip code via `index_code()` but the historical endpoint rejects every one with "Invalid scrip codes", consistently, regardless of date range - a real API limitation, not a config mistake, confirmed by testing each code individually against `/market/historical/1day`. `config/universe.yaml` trimmed to the 2 working indices (also fixing two wrong names it had: the feed calls them "BANK NIFTY" and "Nifty Financial", not "NIFTY BANK"/"NIFTY FIN SERVICE" - same case/wording-sensitivity bug shape as the 2026-09-12 VIX lookup). 29 stocks mapped (5 - the biggest banks - belong to both lists; assigned to the narrower BANK NIFTY only). Both indices loaded, 2,478 real daily bars each, 2016-2026, 0 errors. (2) `data/reports/options_snapshot.jsonl` - new forward-only collection (no historical option-chain endpoint exists, so this is the only way to ever have this data): `broker/indstocks/rest.py` gained `list_expiries()`/`option_chain()` (new, real endpoints - `/market/instruments/expiries`, `/market/option-chain`, documented in docs/indstocks-api.md but previously unused by any client code); `scripts/options_snapshot.py` pulls NIFTY's nearest-weekly chain and stores just `atm_iv`/`put_call_oi_ratio`/`underlying_ltp` (not the raw per-strike payload - nothing to backfill against regardless). Verified live: real first snapshot `atm_iv=10.105`, `put_call_oi_ratio=1.213`. New 9th scheduled task `tradedesk-options-snapshot`, weekdays 13:00 IST. (3) Checkpointed the pre-change training dataset (`data/reports/ml_dataset_checkpoint_pre_phase1.csv`, 29,960 triggered signals) as a fixed before/after baseline.

  **Phase 1 (methodology)**: (1) **Hyperparameter tuning** - all three model families previously used exactly one fixed, guessed config; `train.py` now searches a small grid per family (`_baseline_grid`/`_lightgbm_grid`/`_xgboost_grid` - logistic `C` in 5 values, LightGBM/XGBoost 9 combos each) scored by the same pooled-OOS-Brier mechanism already used for cross-family selection, entirely within the existing purged walk-forward dev folds - the locked final test set is never touched by tuning, same guarantee as before. The winning config's ACTUAL hyperparameters land in the artifact (`ModelBundle.hyperparameters`), not a fixed constant. (2) **Per-setup breakdown** - `per_setup_breakdown()` runs the same `evaluate()`/`select_threshold()` machinery once per setup (`base_breakout`/`trend_pullback`/`nr7_breakout`) instead of only pooling all three into one number, which can hide a real edge in one setup under noise from another; a setup below the `min_above` sample floor is skipped rather than reported with a meaningless single-digit-sample AUC. New `TrainReport.per_setup`/`ModelBundle.per_setup`, printed in `.text()` and saved in the artifact JSON. (3) **Feature-importance stability across folds** - `feature_importance()`/`coefficients()` previously ran once, on the single final refit, which is exactly why the M11 artifact's `rs_percentile` sign anomaly (negative coefficient, backwards from what "relative strength percentile" should predict) could be fold noise or a real problem with no way to tell. `_coefficients_of()`/`_feature_importance_of()` (new free functions, `ModelBundle`'s methods now thin wrappers over them) let the per-fold loop compute importance per fold for the winning model; `_feature_stability()` flags any feature whose sign differs across folds directly in `notes` and saves the per-fold values as `ModelBundle.feature_stability`. (4) **Closed the drift-monitor loop** - `calibration.py::drift_check()` was a pure function nothing in production ever called; new `check_and_flag_drift()` mirrors `signal_tracker.py::flag_setup_failures()`'s exact pattern (rule-based, no LLM, one deduped-by-title `review_queue` item, never auto-flips `config/ml.yaml`) and is wired as `tradedesk ml check-drift`, appended as a **5th step to the existing `tradedesk-after-close` task** (after `scan`) rather than a 10th standalone task, since drift-checking is naturally a daily post-close activity (outcome proxy: NSE paper-book `r_multiple > 0`, not the exact triple-barrier label - documented in the command's own docstring as a deliberate approximation, not exact resolution). Verified live: clean no-op ("no resolved paper trades yet") on the current empty paper book.

  **Phase 2 (sector-return features, built only on what Phase 0 already downloaded)**: `backtest/runner.py::BacktestConfig` gained `sector_codes` (sector name -> that index's own scrip code) alongside the already-existing-but-dead `sector_of` (stock scrip_code -> sector name - a stub nothing ever populated, also used by `backtest/portfolio.py`'s sector-concentration cap, which was silently inert before this for the same reason; populating it here is a welcome side effect, not itself the goal). `prepare_market()` loads each configured sector index's candles into a new `MarketData.sector_candles`. `market_context()` computes `sector_return_1d/5d` from `md.sector_of`/`md.sector_candles`, `None` when a stock has no mapped sector (graceful degradation, same as every other optional context value here). `features.py` **removed `sector_percentile`** (a dead stub that always defaulted to 50.0 - nothing anywhere ever populated it) and added `sector_return_1d/5d` in its place, keeping the feature count from growing open-endedly; **`FEATURE_VERSION` bumped to "v3"**. `predict.py::probability()` now refuses to score a bundle whose `feature_version` doesn't match the current one, rather than silently building a mismatched-column DataFrame. New `cli.py::_sector_config()` resolves `config/sector_membership.yaml` into both `BacktestConfig` fields at each of the 3 real call sites (`scan`, `backtest`, `train` - NSE only, matching the config's scope). `market_context()` also carries a **PLANNED, NOT BUILT** note for the eventual `nifty_atm_iv`/`nifty_pcr` join off Phase 0's options log, with the explicit reason it is not in `FEATURE_NAMES` yet (every already-labelled training row predates the snapshot log, so it would be None for 100% of rows - noise, not a feature).

  **11 new tests** across both phases (`tests/prediction/test_prediction.py`, 59 total in that file, all green). Notably: `market_context()`'s sector-return path has its OWN leakage test (`test_sector_return_never_looks_past_the_arming_date`) - the long-standing generic feature-leakage test cannot reach it, since sector returns arrive at `signal_features()` pre-computed, so the `.loc[:on]` slice inside `market_context()` would otherwise have been an untested leakage surface. Also covered: both sector-return degradation paths (unmapped symbol, mapped-but-no-candles), the stale-`feature_version` refusal (a v2 bundle must raise, not silently misscore; a bundle with NO recorded version is still allowed, for backward compatibility), tuning determinism, and the drift-flag dedupe. The three shadow-gating hard-rule tests (`test_shadow_mode_changes_nothing_but_the_note`, `test_enabled_can_only_lower`, `test_enabled_thresholds`) were deliberately left byte-for-byte untouched and still pass - if any of them had needed editing, that would itself have signalled something leaked outside the prediction layer's intended surface. Full project test suite green after every phase; `uv run ruff check .`/`uv run mypy src` clean throughout.

  **Two real bugs found by actually running this on live NSE data** (2023-09-01 to 2026-09-12, 2,639 codes), not found by unit tests alone: (a) `select_threshold()`'s original "maximise total R" rule had no minimum-sample floor, so on real data where EVERY threshold in the grid showed negative expectancy, it picked 0.80 purely because that threshold happened to leave exactly 1 trade (least to lose, not a real signal) - fixed with a `min_above` floor (default 20) and a `has_edge` flag that goes `False` when even the best eligible threshold is net-negative, so the report says "no threshold shows real improvement" instead of presenting a statistically meaningless pick as a discovery. (b) `equity_curve_from_trades()` initialised every calendar day to `starting_capital` instead of `NaN`, so `.ffill()` had nothing to propagate and any day after the last trade's exit silently reverted to the STARTING capital - combined with plain `pct_change()`'s Jensen's-inequality bias on a lumpy equity curve (a flat round trip up-then-down has a spuriously POSITIVE arithmetic mean return), the real run first reported Sharpe +1.416 alongside CAGR -16% for the SAME losing strategy. Fixed both: the equity curve now starts `NaN` (`.iloc[0]` = capital) before `.ffill()`, and a new `backtest/reports.py::log_returns()` (additive, no Jensen's-inequality bias - a round trip nets to exactly 0) replaces `pct_change()` for Sharpe. Re-run after both fixes: Sharpe -4.701, correctly agreeing in sign with CAGR -16.04%. Regression tests for both: `tests/backtest/test_reports_metrics.py`.

  **The honest finding, now trustworthy** (previously would have been corrupted by bug (b) above): re-running `tradedesk train --from 2023-09-01` end to end on the full real universe gives Strategy A (existing rules, unchanged) 615 closed trades, win rate 26.7%, expectancy -0.557R, net P&L -Rs 41,109, CAGR -16.04%, Sharpe -4.701, 41.1% max drawdown - i.e. **the existing rule-based NSE setups have been net-negative over this specific 2023-2026 window**, independent of any ML question. The model's own OOS metrics (ROC-AUC 0.556, barely above a coin flip; base rate 27%) show weak discriminative power, and no threshold in the standard grid shows positive expectancy with an adequate sample - `has_edge=False`. This mirrors the M13 crypto verdict's honesty (`-0.515R expectancy... do NOT trade these setups`): the ML rigor pass makes the pipeline trustworthy, but trustworthy currently means "confirms there is no edge to filter for" on this window, not "found one." Full artifact: `data/models/20260912-201347.json`; dataset: `data/reports/ml_dataset_v2.csv` (29,834 triggered signals, base rate 0.27).
  **Why the ML layer is stuck at ~0.50-0.556 - diagnosed 2026-09-13, not a methodology gap.** Three measurements on the full universe (30,038 triggered signals, 2023-09 to 2026-09) settled it. The one-off scripts behind these were deleted 2026-09-14 once closed (git history has them); the findings stand, and the locked data they used is closed to further mining.
  1. **80% is unreachable at any exit policy; exits are not the problem.** 36 (target_R x max_hold) cells, 1,079,549 labelled outcomes: hit rate moves 8.6% (3R/hold-3) to 58.0% (0.25R/hold-20), but GROSS expectancy stays pinned in a 0.088R band (-0.192R to -0.280R) across every cell. Hit rate and payoff trade off almost exactly - the signature of no edge: you can dial the hit rate anywhere by moving the target, you cannot dial expectancy. 58% is the measured ceiling and also the worst NET cell (-0.548R), costs dominating a 0.25R target.
  2. **The setups are WORSE THAN RANDOM.** Holding stock, window, stop width (as a fraction of price), target multiple and gap/tie rules fixed and randomising only WHEN you enter: random beats the live setups by **0.206R (1R target) / 0.237R (2R) / 0.256R (3R)**, and random's gross is mildly POSITIVE (+0.035R at 2R) where the setups are -0.203R. Per setup: base_breakout -0.114 (least bad, most selective, 1,391 signals), trend_pullback -0.174, **nr7_breakout -0.255 - and it is 81% of all signals (24,346 of 30,038)**. The first run of this test was WRONG in the null's favour - it sampled random dates from each code's full ~10y history, handing the null the 2020-2024 bull tape the setups never traded; restricting to the real window (a real confound, caught before reporting) moved the 2R edge only -0.265 -> -0.237, so the conclusion held. Slippage (0.05%, ~0.01R) is far too small to explain it. So they are not merely edgeless but anti-predictive: no filter or model on top can fix them, which is exactly why every methodology fix left AUC at ~0.556.
  3. **Costs floor at ~0.18R.** Round-trip drag is a flat 0.268R at the live 0.25%-risk sizing (fixed DP/min-brokerage on ~Rs 13k positions), 0.183R at 0.5% risk, 0.149R at 1% - but 1% implies ~Rs 52k positions, breaching `max_position_value_pct: 0.25`. So ~0.18R of gross edge is needed just to break even, against random's +0.035R.
  **Confirmed on a SECOND exchange (BSE, 2026-09-13)**, which rules out the last plausible excuse - an NSE-specific artifact of that exchange's stocks, liquidity or tape: 34,027 BSE signals, real setups -0.2097R gross at a 24.3% hit rate vs the random null's +0.0339R at 31.3% - edge **-0.2437R** against NSE's -0.237R, with the same per-setup rank order (base_breakout -0.075, trend_pullback -0.137, nr7_breakout -0.230 on 27,500 of 34,027). Different exchange, different universe, same three setups, essentially the same negative number. **The setups themselves are the problem, and that question is now closed.**
  **Exit rule, measured and CHANGED in config.** `partial_at_r 2.0 / partial_fraction 0.5` (half out at 2R, stop to breakeven, runner trails ema10) was worth **-0.114R/trade** versus simply taking the whole position off at T1 - same 612 portfolio-taken trades, -0.5579R realised vs -0.4436R counterfactual, because a label=1 trade realises only +0.844R when the runner half comes back to the breakeven stop. `partial_fraction` is now a real per-setup parameter (previously pinned at the `ExitPlan` default and untunable without editing code) and is **1.0** on all three setups. `backtest/fills.py::evaluate_exit` already handled fraction 1.0 correctly, so no exit logic changed. This does not make the setups profitable - nothing does - it stops giving away 0.114R on every trade taken.
  **The one real gross edge ever found here, and why it is still not tradeable.** `scripts/entry_search.py` is KEPT because it is the reusable instrument, not a closed experiment: it scores ANY candidate entry rule against a matched random-timing null on real data, with a chronological train/test split, Welch's t against the null on the same half, plus an "above ema200 only" control so an uptrend-conditioned rule cannot score merely for being long a rising tape. It asks "does this entry beat a coin flip" BEFORE any model is trained on it. Result: mean reversion ranks clearly above momentum - `rsi2<10 & above ema50` **+0.077R vs the random null, t=7.04** (n_test=10,442), and +0.068R over the ema200 control, which itself scores only +0.0095 (t=1.50) - i.e. being long an uptrend adds nothing and the RSI(2) trigger does all the work. Other significant variants: `rsi2<5 & above ema200` +0.064 (t=5.28), `rsi2<10 & above ema200` +0.053 (t=5.62), `below ema10 & above ema50` +0.035 (t=4.72). Breakout controls sit at or below the null (20d-high -0.031, 20d-low -0.078), consistent with finding (2). Caveat kept: `rsi14>70` also scores +0.059 (t=5.94), so part of what these select for may be "an eventful day" rather than direction - not chased down. Geometry was grid-searched (4 stops x 6 targets x 4 holds) on the TRAIN half only, best cell scored ONCE on the held-out half: **stop 3xATR / target 2R / hold 10 - train gross +0.0725 (t=4.30), locked TEST gross +0.0665 (t=3.35), win rate 52%**. Wider stops win purely because at fixed rupee risk a wider stop means a SMALLER position, shrinking near-fixed DP/min-brokerage costs in R terms. **But it does not survive costs at any sane size.** R is scale-invariant, so risk-per-trade changes only the cost drag, never gross - which answers the sizing question without re-touching the test set: drag 0.128R @0.25% risk, 0.084 @0.5%, 0.066 @0.75%, 0.056 @1%, 0.039 @2%, giving net **-0.018R @0.5% (t=-0.90)**, breakeven ~0.75%, **+0.011R @1% (t=+0.54)**, +0.028R @2%. The gross edge is real and significant; the NET edge is statistically indistinguishable from zero at every size and only turns positive at or beyond risk.yaml's own 1%-max cap. The problem moved from "no edge anywhere" to "a real but sub-cost edge that needs to roughly triple (0.067R -> ~0.20R) to be a business at policy-compliant sizing."
  **Five independent attempts to concentrate or model that edge - ALL negative.** (a) *Filter stacking*: 10 conditions stacked on `rsi2<10 & above ema50` at the locked geometry, selected on TRAIN by net expectancy, best stack scored once on TEST (145,515 simulated entries). Winner was volume confirmation (vol_ratio50>1.2): train net +0.0468 looked strong, **TEST net -0.0005 (t=-0.03)** vs the unfiltered rule's -0.0109 - it moved "modestly negative" to "flat", not to profitable, and the t-stat says don't trust even that. (b) *Model trained ON the RSI(2) population*: 23,305 entries, 30 trailing-window features plus NIFTY/VIX context. Design flaw caught BEFORE fitting: at 3xATR/2R the "hit 2R" label has a **3.3% base rate** (2R on a 3xATR stop is a 6-ATR move inside 10 sessions), so the rule's expectancy comes from TIMEOUT exits and training on that label would predict the wrong event - switched to `label_profit` (base rate 0.509). Logistic beat xgboost on OOS Brier (0.25475 vs 0.25837); dev OOS ROC-AUC 0.515; **LOCKED TEST ROC-AUC 0.4996 - a coin flip**. Gating DEGRADED monotonically as the bar rose (all entries +0.0135, p>=0.50 +0.0192, p>=0.55 +0.0013, p>=0.60 **-0.0198**) - a working ranker improves as you select harder; this does the opposite, so the small p>=0.50 "improvement" is noise. These features do not predict which RSI(2) dips bounce. (c) *Cross-sectional ranking* - whether the RSI(2) value's cross-sectional ORDER carries information, tested against the stronger "random ELIGIBLE stock, same day" null (21,756 trades): K=1 +0.0200R, K=3 -0.0097R, K=5 -0.0033R, K=10 +0.0088R - sign flips across K, all t<2. A 150-code smoke run had shown +0.057R at K=3, pure small-sample noise, gone at full scale. The useful negative buried in it: picking the SINGLE most-oversold eligible stock scores about the same as a RANDOM eligible one, suggesting the edge may live in the ELIGIBILITY FILTER (liquid, priced right, above its own ema50) rather than the RSI(2) trigger. (d) *Long-hold momentum* - real academic momentum on its own terms (buy trailing-return winners, monthly rebalance, hold 3-6 months, raw forward return, no ATR stop, since a stop is not part of that methodology), genuinely untested territory as everything else here caps at 10 sessions: 2,340 codes with history back to 2020, 73 monthly rebalances - **momentum LOSES to a matched random-stock null at all 6 (lookback, hold) cells on the locked half (2025-03 onward), by -0.074R to -0.152R, all six pointing the same way**; the train half looked fine (several t>3) and reversed out of sample. A third independent line of evidence that this market has recently punished, not rewarded, buying strength. Caveat: ~16-month locked window, n=65-80 per cell - the CONSISTENCY across 6 parameterisations is what makes it credible, not multi-cycle proof. (e) *Event features + regime split*, pre-registered with the bar fixed BEFORE looking (AUC >= 0.55; economic claims t >= 2, Bonferroni-corrected across buckets actually tested): **H1 FAILS** - adding results/corporate-action proximity gave locked-test AUC **0.5034** against a 0.4996 baseline. **H2 is NOT a finding despite clearing its number** - the neutral bucket's t=2.39 (bar 2.2) is entirely explained by excluding the test window's risk_off rows, which averaged **-0.30R** on their own, and `regime_size_multiplier: risk_off: 0.0` already sizes those to zero in live/paper trading; within the neutral-only population AUC stayed 0.514. It re-derived a rule the system already had.
  **Standing lesson from all of it**: every further test against the same held-out data raises the multiple-testing burden, so this line was STOPPED and reported rather than reconfigured until something finally printed positive - that is exactly how a false positive gets manufactured. Two honesty notes kept deliberately: the RSI(2) NET edge SIGN FLIPS with the split date (+0.0135 from 2026-01-28 vs -0.0109 from 2025-03-01, neither significant) and quoting whichever window looks better would be cherry-picking; and a real serialization bug surfaced mid-analysis where tagging a null population `"null"` made those rows silently vanish on the CSV round trip, because `pandas.read_csv` treats the literal string "null" as a missing-value sentinel by default (renamed to "random" - it looked exactly like "no null trades were generated" until traced with skip-reason counters).
  **Where this leaves the ML layer**: it was never the broken part - it was pointed at a population that is worse than random, which no filter can rescue. Filtering a population with a POSITIVE base edge is the job it is actually suited to, which is what `eod_learning.py` now does, against forward-collected evidence rather than this closed window.
- **Intraday backfill run and verified (2026-09-13, step 3 of the SDD intraday plan)**: 5 liquid codes (RELIANCE, HDFCBANK, TCS, INFY, SBIN) x 5 intervals (1/3/5/15/60-minute), 2 years, ~2,345 API calls, 0 errors. `scripts/backfill_intraday.py verify` (coverage guard) reports `coverage OK` on every code/interval - full span 2024-09-13 to 2026-09-11, 494-496 sessions each, zero multi-session gaps. Every code showed exactly 2 "thin" sessions clustered on the SAME two calendar dates (2024-11-01 Muhurat trading - NSE's one-hour Diwali session outside normal hours - and 2025-10-21, a genuinely shortened day) - confirmed as real, correctly-fetched short sessions, not a coverage gap; the verify docstring now documents this so a future run does not need to re-investigate. Real bug found and fixed the same run: `run()` called the `verify` Typer command directly as a Python function to auto-verify after fetching, which passed unresolved `typer.Option()` `OptionInfo` objects instead of real values and crashed on the first arithmetic (`int * OptionInfo`) - `typer.Option()` defaults only resolve when Typer's own CLI machinery invokes the command. Fixed by extracting `_verify_impl()` as a plain function taking real values, with `verify()` now a thin CLI wrapper over it; caught by actually running the script against real data, not assumed to work from a smoke test on an empty store. Session VWAP/`intraday_features` (step 2, previously validated only on synthetic data) then checked against this real data across all three of M1/M15/H1: VWAP stays inside its own session's high/low range on every single bar (0 violations across 12,310 M15 + 3,479 H1 + 185,113 M1 bars), and every session's first bar has VWAP exactly equal to that bar's own typical price (0 mismatches) - proving the daily reset actually resets, not just on the synthetic fixture. Next: step 4 (mtf.py + intraday_regime.py) can now build on real stored bars instead of only synthetic ones.
- **Multi-timeframe alignment and intraday regime taxonomy (2026-09-13, step 4)**: `engine/mtf.py::align()` (SDD section 5) - one Direction per timeframe in the SDD's stated hierarchy (1H->15M->5M->3M->1M), an agreement score, a conflict flag with reasons. `last_closed_bar()` is the core discipline: ts is a bar's OPEN time, so a bar only exists in full once `ts + interval.seconds` has elapsed - `ts <= at` alone would let an in-progress bar (missing its own close/high/low) count as known. A missing timeframe is UNKNOWN, never silently NEUTRAL. `engine/intraday_regime.py::classify_intraday_regime()` (SDD section 6) - all ten states from one instrument's own `intraday_features` columns plus two rolling stats computed there (ATR percentile rank, a range-expansion ratio). Real design bug caught by testing, not assumed correct: the first ordering checked volatility percentile BEFORE trend strength, so a strongly trending bar with elevated relative ATR (normal - the move itself IS the volatility) got mislabeled HIGH_VOLATILITY instead of the more informative trend state - a synthetic strong-trend fixture failed under that ordering and made the fix obvious. Reordered: breakout/breakdown first (a prior squeeze, range_contraction<0.7 the bar before, followed by >1.5x range expansion), THEN trend strength (adx>25 + EMA stack = strong; 15-25 + close-vs-ema20 = weak), and only once adx shows no clear trend do volatility/choppy/range apply. Most tests build a synthetic FEATURES frame directly rather than raw OHLCV, isolating each branch deterministically - constructing price series and hoping derived indicators land in the intended band produced several false failures while writing this (a linear price ramp at constant absolute bar range makes atr_pct itself drift monotonically, pre-empting the intended label - a fixture artifact, not a bug, but avoided rather than fought). Both validated against real stored NSE_2885 bars across all 5 backfilled intervals: MTF alignment produces coherent bearish agreement with no conflict; the regime classifier produces all 10 states over the real 2-year history with a sane distribution (breakouts/breakdowns correctly rare: 31 and 29 of 12,310 M15 bars). Neither wired into a scan yet.
- **scan_bar + VWAP Reclaim, the first intraday setup (2026-09-13, step 5)**: `engine/intraday_signals.py` - a PARALLEL model to the daily `Signal`, not a shared one: `armed_on` is a `date` and every hold/exit rule is sessions for the daily model, while an intraday signal arms at a full timestamp and every hold/exit rule is BARS of a specific timeframe (a different number of real minutes per interval) - forcing both into one model risks exactly the kind of unit-mixing bug this project has already hit elsewhere (qty_step/TradeType). `setups/intraday/vwap_reclaim.py::VwapReclaim` implements the SDD's "reclaim VWAP + strong close + volume expansion + retest + continuation": arms when the prior bar was below session VWAP and the current bar closes back above it, with the close in the upper part of its own range (not just scraping over the line) and volume >= a `vol_ratio20` threshold; rejects outright (not merely scores lower) on a conflicting `mtf.Alignment` (SDD section 5) since a VWAP reclaim against a higher-timeframe downtrend is a materially weaker signal. "Retest + continuation" is deliberately NOT hand-rolled inside `arm()` - the trigger is set at the reclaim bar's own high, so the actual entry only fires once a LATER bar continues through it, exactly the same "arm now, confirm later" separation `live/confirmation.py::confirm_trigger` already does for daily signals. `engine/intraday_engine.py::scan_bar()` mirrors `scan_day` structurally: takes an `IntradaySnapshot` (one timeframe, one bar-close `at`, per-code MTF alignment and regime), and - critically - slices every frame to the last CLOSED bar itself via `mtf.last_closed_bar` before any setup sees it, rather than trusting the caller to have already sliced. Proven with a test that plants wildly different future rows in the frame (a 500-vwap, 5-price bar) and confirms the armed signal is byte-identical whether or not they physically exist. Validated against real backfilled data: fires 4 times over RELIANCE's last 300 real M5 bars, a sane frequency for a multi-condition mechanical setup. **This is deliberately UNVALIDATED and UNWIRED**: no null-timing baseline has been run against it (that is step 6, next), it is not in any CLI command, live session, or the eligibility-gate scoring path, and per this session's own hard-won lesson (the three live daily setups are anti-predictive despite plausible-looking win rates) it must not be trusted or alerted on until `null_baseline.py` proves it beats a matched random entry - building the setup and proving it are deliberately kept separate steps.
- **null_baseline.py built and run - VWAP Reclaim FAILS the gate (2026-09-13, step 6)**: `backtest/null_baseline.py::run_null_baseline()` generalises this session's ad hoc null tests (`scripts/barrier_sweep.py`/`entry_search.py`) into reusable, importable machinery: for each real trade, draw `n_cohorts` (default 1000) alternate entries at random bar positions WITHIN THE SAME SESSION on the SAME stock - same day, same stock, same stop distance and target multiple, resolved with the same gap-aware stop-wins-ties barrier used everywhere else in this project, all bounded to the session close (intraday setups never hold overnight). `NullBaselineResult.null_mean_gross_r` is exactly what `TrackRecord.random_baseline_r` should be populated with; 7 tests prove the statistics themselves first (a planted real edge on an otherwise-flat instrument passes with p<0.05; a setup that IS random draws does not; a draw can never cross a session boundary - checked directly, not assumed).
  `scripts/validate_intraday_setup.py` sweeps `VwapReclaim.arm()` across all 5 backfilled codes' full 2-year M5 history, simulates a simple continuation fill (first bar within 5 bars whose high reaches the trigger - the "retest + continuation" the setup itself defers to a later bar), resolves via the session-bounded barrier, and runs the result through the gate. Real result: **1,419 realised trades, mean gross R -0.0051 (net R -0.1635 after intraday costs), null mean gross R -0.0088, edge vs random +0.0037R, p=0.438 - stable across 5 different seeds (0.44-0.47 throughout). DOES NOT beat random timing.** Per the SDD (and this project's now-repeated finding across every setup tried: daily base_breakout/trend_pullback/nr7_breakout, the RSI(2) mean-reversion attempt, and now VWAP Reclaim), this is NO TRADE, not a lowered bar - VwapReclaim stays exactly where it already was: built, tested, and wired nowhere. The −0.1635 net vs −0.0051 gross also confirms the plan's conflict #3 concern directly: intraday round-trip costs are a severe drag relative to a scalp-sized stop, exactly as flagged before any code was written.
  **Where the intraday plan stood after 6 of 7 steps**: the infrastructure (data, VWAP, MTF, regime taxonomy, signal generation, and the validation gate itself) is real, tested and proven against live data at every step. The one thing it was built to find - a tradeable intraday setup - had not been found, on the one candidate tried.
  **Step 7 built and run (2026-09-13) - the observation session, correctly showing NO TRADE on real data.** User explicitly chose to proceed to step 7 even though VwapReclaim had already failed step 6, which reframes what step 7 needed to prove: not "does this setup work" (already answered: no) but "does the full pipeline run correctly end to end and correctly refuse to alert on a setup that hasn't earned it." `live/intraday_observation.py::observe()` walks every bar-close of the arming interval, calls `scan_bar` exactly as a live session would, computes real `mtf.align`/`classify_intraday_regime` context per code, and evaluates each armed signal's SETUP (not each signal individually - the same granularity `journal/stats.py::track_record` already uses) against measured evidence via the exact same `engine/scoring.py::eligibility()` Phase 1 built - one gate, two callers.
  **This module cannot alert, structurally, not just behaviourally**: it has no import of, and no reference to, anything alert-shaped (`alerts/router.py`, Telegram, desktop) - enforced by `test_module_has_no_alert_capability`, which parses the module's own AST and asserts no alert-shaped import exists, so a future edit that added one would fail immediately rather than only being caught by chance on some input.
  A real, instructive test-fixture failure while building this: the first synthetic fixture set every EMA equal, which correctly produced NEUTRAL/UNKNOWN on every timeframe, which correctly made `mtf.align()` report "no defined direction -> conflict", which correctly made `VwapReclaim.arm()` reject every signal before eligibility was ever reached. That was the WIRING working exactly as designed reacting correctly to a bad fixture, not a bug - fixed by giving the fixture a real directional EMA stack, not by loosening the production check.
  **Run against real backfilled data** (`scripts/run_intraday_observation.py`, last 3 real sessions, all 5 codes, full real MTF/regime context computed from the actual 2-year backfill on the other timeframes): 225 real bar-closes scanned, **8 real signals armed** with real trigger/stop prices (e.g. NSE_3045 2026-09-09 09:20 IST, trigger 1001.00/stop 996.62), and **every single one correctly shows NO TRADE** with the real measured reasons (win rate 37.6% < 80% required; beats random by only +0.004R, need +0.100R; 0 out-of-sample trades). Zero would have alerted, and none could have regardless of the eligibility result - the module has no path to one.
  **This closes the intraday plan's 7 steps.** The infrastructure is real, tested at every layer against live data, and the discipline holds end to end: a setup that has not earned the right to alert cannot alert, and the system says exactly why rather than staying silent about the reason. The one thing not yet found is a tradeable intraday setup - VWAP Reclaim was the one candidate built and it failed honestly. The two live setups from the SDD not yet attempted (Support/Resistance Reversal, Breakout+Retest) would go through this exact same gate if tried; nothing about the infrastructure needs to change for that. `config/setups.yaml`, the live daily scan, and the daily paper book remain completely untouched by any of this work.
- M13 (crypto): markets/ has the one real cross-market abstraction, CostModel (NSE's statutory cost shape - STT/stamp/GST/DP/SEBI - has no crypto equivalent; EquityCostModel is a thin wrapper over risk/costs.py, proven byte-identical by property tests). Deliberately NOT built as classes: CalendarProvider/RegimeProvider - data/universe.py::trading_days and engine/regime.py::classify_regime were already market-agnostic (a reference code, an optional VIX series), so wrapping them would be speculative before a second market needed them differently. broker/coindcx/ is read-only, public endpoints, no key: CRITICAL quirk - CoinDCX's candles endpoint silently ignores startTime/endTime unless BOTH are passed together (passing one returns the latest bars regardless, no error) - see broker/coindcx/models.py. CoinDcxClient.candles_history() takes tradedesk scrip codes ("CDX_BTCINR"), NOT CoinDCX's own `pair` ("I-BTC_INR") - unlike IndstocksClient, whose scrip_code already is its native id; needs `register_pairs()` (typically from `CandleStore.custom_symbols()`, which reads the `name` column - Instrument.custom_symbol's storage slot) before use, or every call 422s (a real bug this exact way, found only by actually running `data load --market crypto`, not by mocked tests). data/history_loader.py::load_history takes any MarketDataClient (Protocol) plus max_codes_per_call (CoinDCX: 1 pair/call, no batching) and error_types - defaults preserve NSE behaviour exactly. engine/filters.py::apply_filters takes a CostModel + primitives (min_net_rr, min_avg_daily_turnover_inr, atr_pct_band) instead of RiskConfig/UniverseConfig, so it assumes nothing about which market's config shape it's given; atr_pct_band=None (crypto has no calibrated one yet) skips that check rather than applying NSE's. backtest/portfolio.py::Portfolio.costs and BacktestConfig.costs are CostModel, not ChargeSchedule (None on BacktestConfig -> EquityCostModel(risk.costs), so every existing NSE caller needed zero changes). scan/evening_scan.py's scan_config/build_watchlist/run_evening_scan all take an optional `market: Market` (default NSE, same reasoning). paper/book.py and risk/limits.py stay NSE-only on purpose (crypto has no paper trading or live risk manager yet) - they still build EquityCostModel unconditionally. backtest/runner.py::_dedupe_by_calendar_day guards a real CoinDCX quirk: some 2019-2020 history has a synthetic zero-volume filler bar ~1s before the real bar for that day, which crashes prepare_market's reindex on a duplicate-labeled axis if not deduped (no-op for NSE's clean timestamps). Fractional sizing (2026-09-12): SizeInputs/BacktestConfig/Market carry `qty_step` (1.0 = whole units, the NSE default, so nothing NSE changed - a hypothesis property test asserts step 1.0 reproduces the old math.floor path exactly) and `min_notional`. risk/sizing.py::quantize_qty snaps via Decimal so quantities are clean numbers, not 0.0032200000000000003. Fill.qty/Position.qty_initial/qty_open are float; the partial-exit rule quantizes with ROUND_HALF_EVEN because that is what the old `round()` did at step 1.0. Cost models take `qty: float` and convert with models.py::qty_decimal (str-based - `Decimal * float` raises outright, and `Decimal(0.0032)` expands the binary float). CAVEAT: crypto uses ONE fine step (1e-8) for the whole market, but CoinDCX publishes a step PER PAIR (BTCINR 0.00001 ... DOGEINR 1) plus a per-pair min_quantity - the uniform step moves a quantity by <0.3%, fine for research, NOT sufficient to place a real order. The journal/live NSE path stays whole-unit.
- Health check 2026-09-12 (`data status` staleness line, VIX, stablecoins). Four real defects, all of the same shape - a lookup returned "nothing" and every layer downstream accepted it: (1) **India VIX was never loaded at all.** `data load` appended the benchmark to its targets but not `universe.volatility_index`, AND `CandleStore.scrip_code_for` was case-sensitive while config says "INDIA VIX" and the feed stores "India VIX" - so `cfg.vix_code` was None and engine/regime.py (which takes `vix: pd.Series | None` and degrades quietly) ran WITHOUT its volatility input for the project's whole history. Both fixed; the lookup is now case-insensitive. FOLLOW-UP FIXED SAME DAY: the fail-open this left standing - `vix_calm = vix_last is None or ...` treated a missing reading as calm, so the VIX risk_off trigger could never fire while the data was absent - is now closed by `Market.vix_required` (True for NSE, False for crypto) threaded into `classify_regime(..., vix_required=...)`: when required and missing, the regime now returns RISK_OFF/size 0 with reason "VIX data missing for a market that requires it - failing closed", instead of falling through to a classification that never checked it. `mcp_server.py::analyze` never resolved `cfg.vix_code` either (same bug, different caller) - fixed alongside so it gets a real reading rather than a permanent forced RISK_OFF. (2) **`data load` silently under-fetches.** A run left 739 of 2640 codes (and NIFTY) a session behind with `fetched=0, error=None` - the loader cannot tell "no new bar exists" from "the API returned nothing". Re-running the identical command fetched them all, 0 errors, which is what proves it a fetch gap not missing data. `data load` now does a straggler sweep (retry everything still behind the newest bar, report what remains) and `data status` reports how many codes are BEHIND the newest bar - it previously printed only `max(stamps)`, which hid the gap completely. This supersedes the NIFTY-specific re-fetch workaround in `tradedesk-after-close` (left in place, now redundant). (3) **INR stablecoins polluted the crypto universe.** CDX_USDTINR/CDX_USDCINR are pegged near Rs 98 (2.1% coefficient of variation vs 30-90% for a real coin) so every "breakout" is peg noise - and being the MOST liquid INR pairs, a turnover floor selects FOR them (USDCINR was 51 of 154 signals in the first crypto dataset). `UniverseRules.exclude_codes` (empty for NSE, stablecoins for crypto via `crypto_market.universe.exclude`) drops them in both `universe_on` and `prepare_market`. (4) regime text claimed "VIX calm" for crypto, which has no VIX - now says "no VIX for this market"; and a `size 0:` rejection with no cap fired printed an empty reason, now "risk budget below one qty step".
- pydantic models: Candle, Signal, Position, TradeCard, RiskStatus, RegimeSnapshot, HealthStatus
- One module per setup, implementing the Setup protocol in setups/base.py
- New setups ship disabled until their backtest report and first 30 paper trades are reviewed
